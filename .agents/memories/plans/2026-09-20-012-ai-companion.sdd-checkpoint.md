
# 012 AI Companion - SDD Checkpoint (RESUME from Task 14)

- 2026-09-21 phase wrap after Task 13. Branch v2/lixKRT/012-ai-companion, HEAD=aa3118e, worktree clean, local only (NOT pushed/merged), 011 independent (NOT ancestor). Durable done-list = git log.
- DONE Task 1-13 (+ prereq e5d70f2). Backend 998 tests, frontend 687 tests, tsc + vue-tsc green. Full ledger (volatile): .superpowers/sdd/2026-09-20-012-ai-companion/progress.md.
- RESUME Task 14 (frontend role/intent/privacy/failure tests + mount AiCompanionLauncher in App.vue, no route-meta condition) -> Task 15 (responsive/a11y/browser 320/375/1280) -> Task 16 (whole-feature acceptance + provider reconciliation + memory handoff). Plan: .agents/memories/plans/2026-09-20-012-ai-companion.md.

## Rules (hard)
- model=step-5-preview EXPLICIT on every spawn (effort high); never inherit. AGENTS.md ladder ignored.
- commit type(scope): 中文描述 (feat(ai-companion):/fix(ai-companion):/test(ai-companion):/chore(admin-console):/docs:).
- independent implementer + independent reviewer per task; no self-review; reviewer emits findings + best fix plan; main agent hands plan to implementer; scoped re-review; serial; close completed agents to free slots.
- import message strings from app.ai_companion.constants; 012 backend errors class-attr code; keep two error classes distinct (knowledge-empty vs ai-unavailable).
- frontend: colors only styles/tokens.css (--ark-*), CRLF, 320/375/1280 no orphan/broken CJK; z-index 40 (don't touch .app-header 30 / .onboarding-root 100).
- canUseAiCompanion(role) is role-only (never reads route). resolveCompanionJumpTarget is the only meta.roles user (jump validation only, never visibility).
- ASR reuses 003 get_ai_client().transcribe(audio, filename, call_point=speech_to_text), no dialect param. Frontend voice via 006 VoiceInputButton; recognized text -> store.draft, confirm before submit.
- store useAiCompanionStore(): separate error (chat/voice) and historyError (history-load); activeView store-driven; history lazy-loads on first history-tab switch.

## Deferred to Task 16
- Medium-2 (Task 2): proxy prefilter over-match ("帮忙解释怎么投简历" -> false out_of_scope); fail-safe; decide question-frame guardrail or narrow 帮忙.
- Task 1 Low: ai_companion/__init__.py re-exports 10/12 constants.
- Task 3 Low: whitelist 7-field test hardening.
- Task 5 Info: 180-day keyed on updated_at (record DECISIONS.md); broad except IntegrityError; NotFoundError cross-user regression test.
- Task 6 New-1/New-2: incomplete-record latent 500 (DB-tamper only); learning replay lacks bullets/module_key (no schema column) - use .get defaults.
- Task 11/12/13 Low: tab aria-controls dangling (known tradeoff); dialect arrow-key nav; recognizedText residue. Optional polish.
- 011 integration: 012 app defaults to Unavailable placeholder (real DB provider owned by 011); end-to-end real answers need admin_assistant_feature_knowledge table + real provider WITHOUT admin routes; install ordering to confirm at Task 16/merge.

## Task 14 implementer prompt must include
- Mount AiCompanionLauncher in App.vue (authorized shared-file minimal additive; no route-meta condition). App.test.ts: role x routeMeta x portalRoute matrix -> launcher visible only for 4 non-admin roles (incl. temporary unregistered route + wrong/missing meta.roles). AiCompanionAcceptance.test.ts: intent matrix (3 intents x 3 questions) branch structure; zero apiFetch calls outside the 4 ai-companion URLs; ASR matrix yue/hak/nan via shared endpoint with NO dialect param in body; permission-denied -> no ASR + 未能识别，请重说或改用文字. Verify: npm test, npx tsc -b --noEmit, npm run build.
