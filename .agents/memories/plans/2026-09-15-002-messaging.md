# 02-消息与通知 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 001 已完成账户、会话和角色路由的基础上，交付统一的站内私信、系统通知、订阅推送、全局未读角标及按账号持久化的已读管理。

**Architecture:** 后端在 `backend/app/messaging/` 内新增独立领域模块。SQLite 持久化会话、私信、逐用户消息状态和接收者专属通知；关系矩阵与广播受众由只读查询服务提供；业务模块通过明确的通知事件函数调用 02，不建立独立消息中心。前端通过 Pinia 消息 store 统一读取汇总、会话、线程和通知，在全局头栏显示角标，并由 `/messages` 提供私信与通知的统一操作界面。

**Tech Stack:** Python 3.12、Flask、sqlite3、uv、unittest、Vue 3、Vite、Pinia、Vue Router、Vitest、Vue Test Utils、lucide-vue-next。

**Spec:** `specs/002-messaging-notifications/spec.md`

## Global Constraints

- 账户身份、登录、会话、禁用账户执行和角色路由继续由 01 负责；本 feature 只调用 `load_session()` 与现行角色标识。
- 消息中心仅有私信与系统通知两个顶层类型；业务提醒、公告和订阅推送都属于系统通知分类。
- 私信只允许教师与任意学员、以及已向本企业投递过岗位的学员与企业；其余组合的入口不可见，直接请求必须拒绝。
- 管理员与政府角色可接收系统通知并管理自己的已读状态，但不得参与私信。
- 私信按双人会话线程组织并按发送时间正序展示；发送后不可撤回、编辑，也不得因来源岗位或申请状态变化而删除。
- 教师工作台和企业工作台负责业务动作、状态流转、来源记录及沟通入口资格；02 负责会话、通知送达记录、消息中心、未读总数和已读状态。
- 未读角标等于当前账号未读私信数加未读系统通知数，并在所有角色的全局头栏显示。
- 已读状态按用户账号持久保存，跨登录会话和设备同步。
- “清除已读”只对当前用户隐藏已读条目，不撤回消息、不跨用户删除、不删除未读条目；没有其他消息删除操作。
- 系统通知覆盖审核、求职、公告、兑换履约、积分过期、农事提醒、新政策推送和平台密码重置通知。
- 系统公告发送给触发时存在的全部账号；教学公告发送给触发时存在的全部学员；订阅推送只发送给触发时处于启用订阅状态的学员。
- 渠道仅为站内消息中心；不得发送短信、邮件或其他站外消息。
- 不含 AI 学伴会话、群聊、论坛、消息撤回、消息编辑、附件、图片或语音消息。
- 私信正文和通知正文均为文本；私信正文 trim 后不能为空。
- 所有后端代码位于 `backend/`，所有前端代码位于 `frontend/`；根目录旧系统文件禁止读取、导入、复制或迁移。
- 路由使用 kebab-case，Python 函数使用 snake_case，前端函数和变量使用 camelCase，提交信息使用中文短句。
- 每个任务先建立失败测试，再完成最小实现，再运行定向测试；每个任务结束时提交一个聚焦 commit。
- worktree 固定为 `.worktrees/002-messaging`，分支固定为 `v2/lixKRT/002-messaging-notifications`。
- SDD ledger 固定为 `.superpowers/sdd/2026-09-15-002-messaging/progress.md`。

## Execution Contract

- 实现者：`基元律动/deepseek-flash`，effort `max`。
- 任务级审查：`基元律动/glm-5.3`，effort `high`。
- 修复 4-5 轮：fresh implementer，`基元律动/glm-5.3`，effort `high`。
- 最终全分支审查：`基元律动/glm-5.3`，effort `max`。
- 每次 spawn 必须显式传 `model` 和 `effort`；implementer 不得继续 spawn 子代理。
- 每个任务完成规格合规审查与代码质量审查；Critical 或 Important 未关闭时不得进入下一任务。
- 不得用实现者自述替代真实 diff、测试命令和测试输出。

## File Structure

| Path | Responsibility |
| --- | --- |
| `backend/app/db.py` | 新增消息、通知和跨模块只读关系表 |
| `backend/app/messaging/source_provider.py` | 来源模块只读关系与订阅受众契约 |
| `backend/app/messaging/relationships.py` | 私信关系矩阵与联系人查询 |
| `backend/app/messaging/service.py` | 会话、私信、逐用户状态、未读汇总 |
| `backend/app/messaging/notification_service.py` | 通知写入、幂等去重、通知查询 |
| `backend/app/messaging/events.py` | 审核、求职、履约、积分和密码重置事件接口 |
| `backend/app/messaging/broadcasts.py` | 公告、政策订阅和农事提醒广播 |
| `backend/app/messaging/routes.py` | `/api/messages/*` JSON 路由 |
| `backend/app/__init__.py` | 注册 messaging blueprint 和保护前缀 |
| `backend/tests/test_messaging_foundation.py` | Schema 与约束测试 |
| `backend/tests/test_messaging_access.py` | 允许和禁止关系矩阵测试 |
| `backend/tests/test_private_messages.py` | 会话、发送、回复和线程顺序测试 |
| `backend/tests/test_message_read_state.py` | 角标、已读、全部已读、清除已读测试 |
| `backend/tests/test_notification_service.py` | 通知写入和幂等去重测试 |
| `backend/tests/test_notification_events.py` | 业务通知内容与接收者测试 |
| `backend/tests/test_notification_broadcasts.py` | 公告与订阅受众测试 |
| `backend/tests/test_messaging_api.py` | API 状态码、响应和权限测试 |
| `backend/tests/test_messaging_integration.py` | 端到端消息与通知验收 |
| `frontend/src/api/types.ts` | 消息 DTO 和联合类型 |
| `frontend/src/stores/messages.ts` | 消息状态、加载动作和状态变更 |
| `frontend/src/components/MessageBadge.vue` | 全局未读角标和消息中心入口 |
| `frontend/src/components/AppHeader.vue` | 在所有已认证角色头栏挂载消息入口 |
| `frontend/src/views/MessageCenterView.vue` | 私信与通知统一界面 |
| `frontend/src/router/index.ts` | `/messages` 全角色受保护路由 |
| `frontend/src/data/portal-guides.ts` | 五类门户统一消息中心入口 |

---

### Task 1: Messaging Schema

**Files:**
- Modify: `backend/app/db.py`
- Test: `backend/tests/test_messaging_foundation.py`

**Interfaces:**
- Consumes: `SCHEMA_SQL`, `get_db()`, `init_db()`.
- Produces: tables `message_conversations`, `private_messages`, `private_message_views`, `system_notifications`.

- [ ] **Step 1: Write the failing schema test**

Create `backend/tests/test_messaging_foundation.py`:

```python
import sqlite3
import tempfile
import unittest
from pathlib import Path

from app import create_app
from app.db import get_db


class TestMessagingFoundation(unittest.TestCase):
    def setUp(self):
        self.temp_dir = tempfile.TemporaryDirectory()
        self.app = create_app(
            {
                "TESTING": True,
                "DATABASE_PATH": str(Path(self.temp_dir.name) / "test.db"),
                "SECRET_KEY": "test-only-secret",
            }
        )

    def tearDown(self):
        self.temp_dir.cleanup()

    def test_messaging_tables_and_indexes_exist(self):
        with self.app.app_context():
            db = get_db()
            tables = {
                row["name"]
                for row in db.execute(
                    "SELECT name FROM sqlite_master WHERE type = 'table'"
                )
            }
            indexes = {
                row["name"]
                for row in db.execute(
                    "SELECT name FROM sqlite_master WHERE type = 'index'"
                )
            }
        self.assertTrue(
            {
                "message_conversations",
                "private_messages",
                "private_message_views",
                "system_notifications",
            }.issubset(tables)
        )
        self.assertTrue(
            {
                "idx_private_messages_conversation",
                "idx_private_views_unread",
                "idx_notifications_unread",
            }.issubset(indexes)
        )

    def test_conversation_requires_ordered_unique_pair(self):
        now = "2026-09-15T00:00:00+00:00"
        with self.app.app_context():
            db = get_db()
            db.executemany(
                """
                INSERT INTO users (
                    username, password_hash, name, role, is_enabled,
                    created_at, updated_at
                )
                VALUES (?, 'hash', ?, 'student', 1, ?, ?)
                """,
                [("s1", "学员一", now, now), ("s2", "学员二", now, now)],
            )
            db.execute(
                """
                INSERT INTO message_conversations (
                    participant_low_id, participant_high_id, created_at, updated_at
                )
                VALUES (1, 2, ?, ?)
                """,
                (now, now),
            )
            with self.assertRaises(sqlite3.IntegrityError):
                db.execute(
                    """
                    INSERT INTO message_conversations (
                        participant_low_id, participant_high_id, created_at, updated_at
                    )
                    VALUES (2, 1, ?, ?)
                    """,
                    (now, now),
                )


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `uv run --directory backend python -m unittest tests.test_messaging_foundation -v`

Expected: FAIL because the messaging tables do not exist.

- [ ] **Step 3: Add the schema**

Append these statements to `SCHEMA_SQL` in `backend/app/db.py`:

```sql
CREATE TABLE IF NOT EXISTS message_conversations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    participant_low_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    participant_high_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    CHECK (participant_low_id < participant_high_id),
    UNIQUE (participant_low_id, participant_high_id)
);

CREATE TABLE IF NOT EXISTS private_messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    conversation_id INTEGER NOT NULL
        REFERENCES message_conversations(id) ON DELETE CASCADE,
    sender_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    body TEXT NOT NULL CHECK (length(trim(body)) > 0),
    created_at TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_private_messages_conversation
    ON private_messages(conversation_id, created_at, id);

CREATE TABLE IF NOT EXISTS private_message_views (
    message_id INTEGER NOT NULL REFERENCES private_messages(id) ON DELETE CASCADE,
    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    read_at TEXT,
    cleared_at TEXT,
    PRIMARY KEY (message_id, user_id)
);

CREATE INDEX IF NOT EXISTS idx_private_views_unread
    ON private_message_views(user_id, read_at, cleared_at);

CREATE TABLE IF NOT EXISTS system_notifications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    recipient_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    event_key TEXT NOT NULL,
    event_type TEXT NOT NULL,
    title TEXT NOT NULL,
    body TEXT NOT NULL,
    source_type TEXT,
    source_id TEXT,
    source_available INTEGER NOT NULL DEFAULT 1 CHECK (source_available IN (0, 1)),
    created_at TEXT NOT NULL,
    read_at TEXT,
    cleared_at TEXT,
    UNIQUE (recipient_id, event_key)
);

CREATE INDEX IF NOT EXISTS idx_notifications_unread
    ON system_notifications(recipient_id, read_at, cleared_at, created_at DESC);
```

- [ ] **Step 4: Run the foundation test**

Run: `uv run --directory backend python -m unittest tests.test_messaging_foundation -v`

Expected: 2 tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/db.py backend/tests/test_messaging_foundation.py
git commit -m "feat(messaging): 建立消息通知数据基础"
```

---

### Task 2: Private-Message Relationship Matrix and Contact Discovery

**Files:**
- Create: `backend/app/messaging/__init__.py`
- Create: `backend/app/messaging/source_provider.py`
- Create: `backend/app/messaging/relationships.py`
- Test: `backend/tests/test_messaging_access.py`

**Interfaces:**
- Consumes: `get_db()`; `users`; `MessagingSourceProvider`.
- Produces:
  - `MessagingSourceProvider` protocol with `has_application_relationship(student_id: int, enterprise_id: int) -> bool`, `list_applied_enterprise_ids(student_id: int) -> list[int]`, `list_applicant_student_ids(enterprise_id: int) -> list[int]`, `list_policy_subscriber_ids(category: str) -> list[int]`, and `list_product_subscriber_ids(product_key: str) -> list[int]`
  - `NullMessagingSourceProvider`
  - `set_messaging_source_provider(app: Flask, provider: MessagingSourceProvider) -> None`
  - `get_messaging_source_provider() -> MessagingSourceProvider`
  - `application_relationship_exists(student_id: int, enterprise_id: int) -> bool`
  - `messaging_relationship(viewer_id: int, other_id: int) -> str | None`
  - `list_allowed_contacts(user_id: int) -> list[dict]`
  - Contact shape: `{"id": int, "name": str, "role": str, "relationship": "teacher_student" | "application"}`.

- [ ] **Step 1: Write the failing relationship tests**

Create `backend/tests/test_messaging_access.py` with two teachers, two students, two enterprises, one administrator, and one government account. Register this fake source provider in `setUp()` instead of creating a source business table:

```python
class FakeMessagingSourceProvider:
    def __init__(self):
        self.applications = {(1, 3)}

    def has_application_relationship(self, student_id: int, enterprise_id: int) -> bool:
        return (student_id, enterprise_id) in self.applications

    def list_applied_enterprise_ids(self, student_id: int) -> list[int]:
        return sorted(
            enterprise_id
            for current_student, enterprise_id in self.applications
            if current_student == student_id
        )

    def list_applicant_student_ids(self, enterprise_id: int) -> list[int]:
        return sorted(
            student_id
            for student_id, current_enterprise in self.applications
            if current_enterprise == enterprise_id
        )

    def list_policy_subscriber_ids(self, category: str) -> list[int]:
        return []

    def list_product_subscriber_ids(self, product_key: str) -> list[int]:
        return []
```

Assert:

```python
def test_student_enterprise_requires_an_application(self):
    self.assertEqual(
        messaging_relationship(self.student_a, self.enterprise_with_application),
        "application",
    )
    self.assertIsNone(
        messaging_relationship(self.student_a, self.enterprise_without_application)
    )

def test_teacher_and_student_are_always_allowed(self):
    self.assertEqual(
        messaging_relationship(self.teacher_a, self.student_b),
        "teacher_student",
    )
    self.assertEqual(
        messaging_relationship(self.student_b, self.teacher_a),
        "teacher_student",
    )

def test_contacts_are_exactly_allowed_and_unique(self):
    self.assertEqual(
        {item["id"] for item in list_allowed_contacts(self.student_a)},
        {
            self.teacher_a,
            self.teacher_b,
            self.enterprise_with_application,
        },
    )
    self.assertEqual(list_allowed_contacts(self.admin_user), [])
```

Also assert all same-role pairs, enterprise to unapplied student, and administrator/government private pairs return `None`.

- [ ] **Step 2: Run the tests to verify they fail**

Run: `uv run --directory backend python -m unittest tests.test_messaging_access -v`

Expected: FAIL because `app.messaging.relationships` does not exist.

- [ ] **Step 3: Implement relationship queries**

Create an empty `backend/app/messaging/__init__.py`.

Create `backend/app/messaging/source_provider.py`:

```python
from __future__ import annotations

from typing import Protocol

from flask import Flask, current_app


class MessagingSourceProvider(Protocol):
    def has_application_relationship(
        self, student_id: int, enterprise_id: int
    ) -> bool: ...

    def list_applied_enterprise_ids(self, student_id: int) -> list[int]: ...

    def list_applicant_student_ids(self, enterprise_id: int) -> list[int]: ...

    def list_policy_subscriber_ids(self, category: str) -> list[int]: ...

    def list_product_subscriber_ids(self, product_key: str) -> list[int]: ...


class NullMessagingSourceProvider:
    def has_application_relationship(
        self, student_id: int, enterprise_id: int
    ) -> bool:
        return False

    def list_applied_enterprise_ids(self, student_id: int) -> list[int]:
        return []

    def list_applicant_student_ids(self, enterprise_id: int) -> list[int]:
        return []

    def list_policy_subscriber_ids(self, category: str) -> list[int]:
        return []

    def list_product_subscriber_ids(self, product_key: str) -> list[int]:
        return []


def set_messaging_source_provider(
    app: Flask, provider: MessagingSourceProvider
) -> None:
    app.extensions["messaging_source_provider"] = provider


def get_messaging_source_provider() -> MessagingSourceProvider:
    return current_app.extensions.get(
        "messaging_source_provider",
        NullMessagingSourceProvider(),
    )
```

Create `backend/app/messaging/relationships.py`:

```python
from __future__ import annotations

from app.db import get_db
from app.messaging.source_provider import get_messaging_source_provider


def application_relationship_exists(student_id: int, enterprise_id: int) -> bool:
    return get_messaging_source_provider().has_application_relationship(
        student_id,
        enterprise_id,
    )


def messaging_relationship(viewer_id: int, other_id: int) -> str | None:
    if viewer_id == other_id:
        return None
    roles = {
        int(row["id"]): str(row["role"])
        for row in get_db().execute(
            "SELECT id, role FROM users WHERE id IN (?, ?)",
            (viewer_id, other_id),
        ).fetchall()
    }
    if len(roles) != 2:
        return None
    viewer_role, other_role = roles[viewer_id], roles[other_id]
    if {viewer_role, other_role} == {"teacher", "student"}:
        return "teacher_student"
    if viewer_role == "student" and other_role == "enterprise":
        return (
            "application"
            if application_relationship_exists(viewer_id, other_id)
            else None
        )
    if viewer_role == "enterprise" and other_role == "student":
        return (
            "application"
            if application_relationship_exists(other_id, viewer_id)
            else None
        )
    return None


def list_allowed_contacts(user_id: int) -> list[dict]:
    user = get_db().execute(
        "SELECT role FROM users WHERE id = ? AND is_enabled = 1",
        (user_id,),
    ).fetchone()
    if user is None:
        return []
    role = str(user["role"])
    if role == "student":
        teacher_rows = get_db().execute(
            """
            SELECT id, name, role, 'teacher_student' AS relationship
            FROM users
            WHERE role = 'teacher' AND is_enabled = 1
            ORDER BY id
            """
        ).fetchall()
        enterprise_ids = (
            get_messaging_source_provider().list_applied_enterprise_ids(user_id)
        )
        enterprise_rows = self_contact_rows(
            enterprise_ids,
            "application",
        )
        rows = [*teacher_rows, *enterprise_rows]
    elif role == "teacher":
        rows = get_db().execute(
            """
            SELECT id, name, role, 'teacher_student' AS relationship
            FROM users
            WHERE role = 'student' AND is_enabled = 1
            ORDER BY id
            """
        ).fetchall()
    elif role == "enterprise":
        student_ids = (
            get_messaging_source_provider().list_applicant_student_ids(user_id)
        )
        rows = self_contact_rows(student_ids, "application")
    else:
        return []
    return [
        {
            "id": int(row["id"]),
            "name": str(row["name"]),
            "role": str(row["role"]),
            "relationship": str(row["relationship"]),
        }
        for row in rows
    ]
```

Define `self_contact_rows(user_ids: list[int], relationship: str) -> list[dict]` in the same module. It must query enabled users with `SELECT id, name, role FROM users WHERE id IN (<placeholders>) AND is_enabled = 1`, preserve the provider ID order, and return dictionaries with the supplied relationship. Return `[]` when `user_ids` is empty.

- [ ] **Step 4: Run the relationship tests**

Run: `uv run --directory backend python -m unittest tests.test_messaging_access -v`

Expected: all relationship tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/messaging/__init__.py backend/app/messaging/relationships.py backend/tests/test_messaging_access.py
git commit -m "feat(messaging): 实现私信关系矩阵"
```

---

### Task 3: Private Conversations, Threads, Sending, and Replies

**Files:**
- Create: `backend/app/messaging/service.py`
- Test: `backend/tests/test_private_messages.py`

**Interfaces:**
- Consumes: `messaging_relationship()`; `get_db()`.
- Produces:
  - `MessageValidationError(ValueError)` with `errors: dict[str, str]`
  - `MessagingAccessDeniedError(PermissionError)`
  - `ConversationNotFoundError(LookupError)`
  - `validate_message_body(body: object) -> str`
  - `send_private_message(sender_id: int, recipient_id: int, body: object) -> dict`
  - `reply_to_conversation(sender_id: int, conversation_id: int, body: object) -> dict`
  - `list_conversations(user_id: int) -> list[dict]`
  - `get_conversation_messages(user_id: int, conversation_id: int) -> dict`

- [ ] **Step 1: Write the failing private-message tests**

Create `backend/tests/test_private_messages.py`. Cover whitespace validation, prohibited first contact, conversation reuse, message order, enterprise reply with a provider-confirmed application relationship, existing-thread reply after the provider relationship is removed, and non-participant rejection.

Include:

```python
def test_repeated_send_reuses_one_conversation(self):
    first = send_private_message(self.teacher_id, self.student_id, "第一条")
    second = send_private_message(self.teacher_id, self.student_id, "第二条")
    self.assertEqual(first["conversation"]["id"], second["conversation"]["id"])
    thread = get_conversation_messages(
        self.student_id, first["conversation"]["id"]
    )
    self.assertEqual(
        [item["body"] for item in thread["messages"]],
        ["第一条", "第二条"],
    )

def test_prohibited_message_creates_no_rows(self):
    with self.assertRaises(MessagingAccessDeniedError):
        send_private_message(self.student_a_id, self.student_b_id, "不允许")
    with self.app.app_context():
        self.assertEqual(
            get_db().execute(
                "SELECT COUNT(*) AS count FROM message_conversations"
            ).fetchone()["count"],
            0,
        )
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `uv run --directory backend python -m unittest tests.test_private_messages -v`

Expected: FAIL because `app.messaging.service` does not exist.

- [ ] **Step 3: Implement private conversations**

Create `backend/app/messaging/service.py` with UTC ISO timestamps and:

```python
def validate_message_body(body: object) -> str:
    normalized = str(body or "").strip()
    if not normalized:
        raise MessageValidationError({"body": "消息内容不能为空"})
    return normalized


def _ordered_pair(user_a_id: int, user_b_id: int) -> tuple[int, int]:
    low, high = sorted((user_a_id, user_b_id))
    return low, high
```

Implement `send_private_message()` in one transaction:

1. Validate the body.
2. Reject when `messaging_relationship()` returns `None`.
3. Insert or select the conversation using the ordered pair.
4. Insert `private_messages`.
5. Insert sender and recipient `private_message_views`; sender receives `read_at=created_at`, recipient receives `NULL`.
6. Update `message_conversations.updated_at`.
7. Return `{"conversation": conversation_payload, "message": message_payload}`.

Implement `reply_to_conversation()` by verifying the sender is a participant, inserting the message and two views, and updating the conversation timestamp. Do not re-check the application relationship for an existing thread.

Implement `list_conversations()` so it returns only conversations where the user has a visible message, resolves the other participant, returns the latest visible message, returns unread count, and orders by `updated_at DESC, id DESC`.

Implement `get_conversation_messages()` so it returns only messages not cleared for the current user, ordered by `created_at ASC, private_messages.id ASC`, with `read: bool`.

- [ ] **Step 4: Run the private-message tests**

Run: `uv run --directory backend python -m unittest tests.test_private_messages -v`

Expected: all private-message tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/messaging/service.py backend/tests/test_private_messages.py
git commit -m "feat(messaging): 实现站内私信会话"
```

---

### Task 4: Account-Level Read State, Summary, and Clear-Read Behavior

**Files:**
- Modify: `backend/app/messaging/service.py`
- Test: `backend/tests/test_message_read_state.py`

**Interfaces:**
- Consumes: Task 3 conversation tables; `system_notifications`.
- Produces:
  - `MessageNotFoundError(LookupError)`
  - `get_message_summary(user_id: int) -> dict`
  - `mark_private_message_read(user_id: int, message_id: int) -> dict`
  - `mark_notification_read(user_id: int, notification_id: int) -> dict`
  - `mark_all_read(user_id: int) -> dict`
  - `clear_read_items(user_id: int) -> dict`

- [ ] **Step 1: Write the failing read-state tests**

Create `backend/tests/test_message_read_state.py`:

```python
def test_sending_only_increments_recipient_unread(self):
    send_private_message(self.teacher_id, self.student_id, "请查看")
    self.assertEqual(get_message_summary(self.teacher_id)["unread_total"], 0)
    self.assertEqual(
        get_message_summary(self.student_id)["unread_private"],
        1,
    )

def test_single_read_changes_only_one_item(self):
    sent = send_private_message(self.teacher_id, self.student_id, "请查看")
    mark_private_message_read(self.student_id, sent["message"]["id"])
    self.assertEqual(get_message_summary(self.student_id)["unread_total"], 0)

def test_clear_read_keeps_unread_and_hides_only_current_account(self):
    read = send_private_message(self.teacher_id, self.student_id, "已读")
    unread = send_private_message(self.teacher_id, self.student_id, "未读")
    mark_private_message_read(self.student_id, read["message"]["id"])
    clear_read_items(self.student_id)
    thread = get_conversation_messages(
        self.student_id, read["conversation"]["id"]
    )
    self.assertEqual(
        [item["id"] for item in thread["messages"]],
        [unread["message"]["id"]],
    )
    self.assertEqual(
        get_message_summary(self.student_id)["unread_private"],
        1,
    )
```

Add a test that calls `get_message_summary()` from a second app context and verifies the read state persists.

Add these two tests before Step 2:

```python
def test_new_message_after_clear_reopens_thread_with_only_new_item_visible(self):
    first = send_private_message(self.teacher_id, self.student_id, "旧消息")
    mark_private_message_read(self.student_id, first["message"]["id"])
    clear_read_items(self.student_id)
    self.assertEqual(
        list_conversations(self.student_id),
        [],
    )

    second = send_private_message(self.teacher_id, self.student_id, "新消息")
    conversations = list_conversations(self.student_id)
    self.assertEqual(len(conversations), 1)
    self.assertEqual(conversations[0]["unread_count"], 1)
    thread = get_conversation_messages(
        self.student_id,
        first["conversation"]["id"],
    )
    self.assertEqual(
        [item["id"] for item in thread["messages"]],
        [second["message"]["id"]],
    )

def test_clear_read_handles_notifications_without_clearing_unread(self):
    now = "2026-09-15T00:00:00+00:00"
    with self.app.app_context():
        db = get_db()
        db.executemany(
            """
            INSERT INTO system_notifications (
                recipient_id, event_key, event_type, title, body, created_at, read_at
            )
            VALUES (?, ?, 'test', '标题', '正文', ?, ?)
            """,
            [
                (self.student_id, "read-event", now, now),
                (self.student_id, "unread-event", now, None),
            ],
        )
        db.commit()

    result = clear_read_items(self.student_id)
    self.assertEqual(result["cleared_notifications"], 1)
    self.assertEqual(get_message_summary(self.student_id)["unread_notifications"], 1)
    with self.app.app_context():
        rows = get_db().execute(
            """
            SELECT event_key, cleared_at
            FROM system_notifications
            WHERE recipient_id = ?
            ORDER BY event_key
            """,
            (self.student_id,),
        ).fetchall()
    self.assertIsNotNone(rows[0]["cleared_at"])
    self.assertIsNone(rows[1]["cleared_at"])
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `uv run --directory backend python -m unittest tests.test_message_read_state -v`

Expected: FAIL because summary and read-state functions do not exist.

- [ ] **Step 3: Implement read state**

Add to `backend/app/messaging/service.py`:

```python
def get_message_summary(user_id: int) -> dict:
    unread_private = get_db().execute(
        """
        SELECT COUNT(*) AS count
        FROM private_message_views
        WHERE user_id = ? AND read_at IS NULL AND cleared_at IS NULL
        """,
        (user_id,),
    ).fetchone()["count"]
    unread_notifications = get_db().execute(
        """
        SELECT COUNT(*) AS count
        FROM system_notifications
        WHERE recipient_id = ? AND read_at IS NULL AND cleared_at IS NULL
        """,
        (user_id,),
    ).fetchone()["count"]
    return {
        "unread_private": int(unread_private),
        "unread_notifications": int(unread_notifications),
        "unread_total": int(unread_private) + int(unread_notifications),
    }
```

`mark_private_message_read()` updates only the matching current-user view and returns `get_message_summary(user_id)`.

`mark_notification_read()` updates only the matching recipient row and returns the updated summary. A missing or foreign ID raises `MessageNotFoundError`.

`mark_all_read()` sets `read_at` on every unread private view and notification for the account, returns the updated summary, and commits once.

`clear_read_items()` sets `cleared_at` only where `read_at IS NOT NULL AND cleared_at IS NULL`; it returns:

```python
{
    "cleared_private": int,
    "cleared_notifications": int,
    "unread_total": get_message_summary(user_id)["unread_total"],
}
```

- [ ] **Step 4: Run the read-state tests**

Run: `uv run --directory backend python -m unittest tests.test_message_read_state -v`

Expected: all read-state tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/messaging/service.py backend/tests/test_message_read_state.py
git commit -m "feat(messaging): 实现未读角标与已读管理"
```

---

### Task 5: Idempotent System Notification Delivery

**Files:**
- Create: `backend/app/messaging/notification_service.py`
- Test: `backend/tests/test_notification_service.py`

**Interfaces:**
- Consumes: `system_notifications`, `users`.
- Produces:
  - `NotificationValidationError(ValueError)`
  - `emit_notification(*, recipient_id: int, event_key: str, event_type: str, title: str, body: str, source_type: str | None = None, source_id: str | None = None, source_available: bool = True) -> dict`
  - `emit_notifications(*, recipient_ids: list[int], event_key: str, event_type: str, title: str, body: str, source_type: str | None = None, source_id: str | None = None, source_available: bool = True) -> dict`
  - `mark_notification_sources_unavailable(*, source_type: str, source_id: str) -> int`
  - `list_notifications(user_id: int) -> list[dict]`

- [ ] **Step 1: Write the failing notification tests**

Create `backend/tests/test_notification_service.py`:

```python
def test_same_event_key_is_delivered_once(self):
    first = emit_notification(
        recipient_id=self.student_id,
        event_key="review_approved:submission-4",
        event_type="review_approved",
        title="审核通过",
        body="课程视频已通过并上架",
        source_type="course_video",
        source_id="88",
    )
    second = emit_notification(
        recipient_id=self.student_id,
        event_key="review_approved:submission-4",
        event_type="review_approved",
        title="审核通过",
        body="课程视频已通过并上架",
        source_type="course_video",
        source_id="88",
    )
    self.assertEqual(first["id"], second["id"])
    self.assertEqual(len(list_notifications(self.student_id)), 1)

def test_new_event_key_creates_new_notification(self):
    common = {
        "recipient_id": self.student_id,
        "event_type": "review_approved",
        "title": "审核通过",
        "body": "课程视频已通过并上架",
    }
    first = emit_notification(event_key="review_approved:submission-4", **common)
    second = emit_notification(event_key="review_approved:submission-5", **common)
    self.assertNotEqual(first["id"], second["id"])
```

Also assert blank fields raise `NotificationValidationError`, unrelated accounts receive nothing, and `emit_notifications()` deduplicates repeated recipient IDs.

- [ ] **Step 2: Run the tests to verify they fail**

Run: `uv run --directory backend python -m unittest tests.test_notification_service -v`

Expected: FAIL because `app.messaging.notification_service` does not exist.

- [ ] **Step 3: Implement idempotent notification delivery**

Create `backend/app/messaging/notification_service.py`.

For each recipient run:

```sql
INSERT INTO system_notifications (
    recipient_id, event_key, event_type, title, body,
    source_type, source_id, source_available, created_at
)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
ON CONFLICT (recipient_id, event_key) DO NOTHING
```

Then select by `(recipient_id, event_key)` and return the persisted row as a dictionary. The insert and select run inside one transaction.

`emit_notifications()` deduplicates recipient IDs, calls the same persistence primitive for each recipient, and returns:

```python
{
    "created_count": len(records),
    "unique_recipient_count": len(unique_ids),
    "notifications": records,
}
```

`list_notifications()` selects only the current user’s uncleared rows, ordered by `created_at DESC, id DESC`, with `read: bool`.

`mark_notification_sources_unavailable()` updates matching historical notifications to `source_available=0` and returns the number of affected recipient rows. The owning business module calls this contract when its source object becomes unavailable; 02 never guesses source state.

- [ ] **Step 4: Run the notification-service tests**

Run: `uv run --directory backend python -m unittest tests.test_notification_service -v`

Expected: all notification-service tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/messaging/notification_service.py backend/tests/test_notification_service.py
git commit -m "feat(messaging): 实现幂等系统通知"
```

---

### Task 6: Review, Application, Fulfillment, Points, and Password Event Adapters

**Files:**
- Create: `backend/app/messaging/events.py`
- Test: `backend/tests/test_notification_events.py`

**Interfaces:**
- Consumes: `emit_notification()`, `emit_notifications()`.
- Produces:
  - `emit_review_result(*, event_id: str, submitter_id: int, content_type: str, content_id: str, approved: bool, opinion: str | None = None) -> dict`
  - `emit_application_submitted(*, event_id: str, enterprise_id: int, student_id: int, application_id: str, student_name: str, job_title: str) -> dict`
  - `emit_application_status_changed(*, event_id: str, student_id: int, application_id: str, status: str) -> dict`
  - `emit_position_closed(*, event_id: str, student_ids: list[int], position_id: str, job_title: str) -> dict`
  - `emit_redemption_succeeded(*, event_id: str, student_id: int, redemption_id: str, points_spent: int, prize_name: str) -> dict`
  - `emit_fulfillment_issued(*, event_id: str, student_id: int, fulfillment_id: str, prize_name: str) -> dict`
  - `emit_fulfillment_cancelled(*, event_id: str, student_id: int, fulfillment_id: str, prize_name: str, restored_points: int) -> dict`
  - `emit_points_expired(*, event_id: str, student_id: int, points_cleared: int) -> dict`
  - `emit_password_reset(*, event_id: str, user_id: int) -> dict`

- [ ] **Step 1: Write the failing event tests**

Create `backend/tests/test_notification_events.py`. For each adapter assert event type, source type/id, intended recipients, required content, and same-event retry idempotency.

Use these assertions:

```python
self.assertIn("已通过并上架", approval["title"] + approval["body"])
self.assertIn("审核意见：画面清晰", rejection["title"] + rejection["body"])
self.assertIn("张同学", enterprise_notice["title"] + enterprise_notice["body"])
self.assertIn("农业技术员", enterprise_notice["title"] + enterprise_notice["body"])
self.assertIn("意向沟通", status_notice["title"] + status_notice["body"])
self.assertIn("岗位已关闭", closed_notice["title"] + closed_notice["body"])
self.assertIn("120积分", redemption["title"] + redemption["body"])
self.assertIn("保温杯", redemption["title"] + redemption["body"])
self.assertIn("积分已回退", cancellation["title"] + cancellation["body"])
self.assertIn("30积分清零", expiration["title"] + expiration["body"])
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `uv run --directory backend python -m unittest tests.test_notification_events -v`

Expected: FAIL because `app.messaging.events` does not exist.

- [ ] **Step 3: Implement event adapters**

Create `backend/app/messaging/events.py`. Every adapter uses `event_key=f"{event_type}:{event_id}"`.

Use these event types:

```python
REVIEW_APPROVED = "review_approved"
REVIEW_REJECTED = "review_rejected"
APPLICATION_SUBMITTED = "application_submitted"
APPLICATION_STATUS = "application_status"
POSITION_CLOSED = "position_closed"
REDEMPTION_SUCCEEDED = "redemption_succeeded"
FULFILLMENT_ISSUED = "fulfillment_issued"
FULFILLMENT_CANCELLED = "fulfillment_cancelled"
POINTS_EXPIRED = "points_expired"
PASSWORD_RESET = "password_reset"
```

Reject review rejection when `opinion` is blank. Application submission includes student name and job title. Position closure uses `emit_notifications()` for all affected students. Redemption, fulfillment, and points events include the exact numeric value followed by `积分`. Password reset creates title `密码已重置` and body `请联系管理员获取初始密码`; it does not modify credentials.

- [ ] **Step 4: Run the event tests**

Run: `uv run --directory backend python -m unittest tests.test_notification_events -v`

Expected: all event tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/messaging/events.py backend/tests/test_notification_events.py
git commit -m "feat(messaging): 接入业务事件通知"
```

---

### Task 7: Announcements and Subscription Delivery

**Files:**
- Create: `backend/app/messaging/broadcasts.py`
- Test: `backend/tests/test_notification_broadcasts.py`

**Interfaces:**
- Consumes: `emit_notifications()`; `get_messaging_source_provider()`.
- Produces:
  - `emit_system_announcement(*, event_id: str, announcement_id: str, title: str, body: str) -> dict`
  - `emit_teaching_announcement(*, event_id: str, teacher_id: int, announcement_id: str, title: str, body: str) -> dict`
  - `emit_policy_published(*, event_id: str, policy_id: str, title: str, category: str) -> dict`
  - `emit_monthly_agriculture_reminder(*, event_id: str, product_key: str, month: str, body: str) -> dict`

- [ ] **Step 1: Write the failing broadcast tests**

Create `backend/tests/test_notification_broadcasts.py` with all six roles and a fake provider that stores mutable policy and product subscriber ID sets:

```python
def test_system_announcement_reaches_every_existing_account(self):
    result = emit_system_announcement(
        event_id="announcement-1",
        announcement_id="a1",
        title="系统维护",
        body="今晚进行维护",
    )
    self.assertEqual(result["unique_recipient_count"], len(self.all_user_ids))

def test_teaching_announcement_reaches_students_only(self):
    result = emit_teaching_announcement(
        event_id="teaching-1",
        teacher_id=self.teacher_id,
        announcement_id="t1",
        title="课程安排",
        body="本周课程调整",
    )
    self.assertEqual(self.recipient_ids(result), set(self.student_ids))

def test_policy_push_uses_active_subscribers(self):
    result = emit_policy_published(
        event_id="policy-1",
        policy_id="p1",
        title="创业补贴",
        category="创业支持",
    )
    self.assertEqual(
        self.recipient_ids(result),
        {self.subscribed_student_id},
    )

def test_unsubscribe_keeps_existing_notification_and_stops_future_push(self):
    first = emit_policy_published(
        event_id="policy-1",
        policy_id="p1",
        title="创业补贴",
        category="创业支持",
    )
    self.assertEqual(
        self.recipient_ids(first),
        {self.subscribed_student_id},
    )

    self.provider.policy_subscribers["创业支持"].remove(
        self.subscribed_student_id
    )
    second = emit_policy_published(
        event_id="policy-2",
        policy_id="p2",
        title="第二期补贴",
        category="创业支持",
    )

    self.assertEqual(self.recipient_ids(second), set())
    self.assertEqual(
        len(list_notifications(self.subscribed_student_id)),
        1,
    )
```

Add agriculture reminder assertions for active product subscription, exact product, and exact month.

- [ ] **Step 2: Run the tests to verify they fail**

Run: `uv run --directory backend python -m unittest tests.test_notification_broadcasts -v`

Expected: FAIL because `app.messaging.broadcasts` does not exist.

- [ ] **Step 3: Implement audience delivery**

Create `backend/app/messaging/broadcasts.py`.

Use exact audience sources:

```python
provider = get_messaging_source_provider()
all_user_ids = [
    int(row["id"])
    for row in get_db().execute("SELECT id FROM users ORDER BY id").fetchall()
]
student_ids = [
    int(row["id"])
    for row in get_db().execute(
        "SELECT id FROM users WHERE role = 'student' ORDER BY id"
    ).fetchall()
]
policy_subscriber_ids = provider.list_policy_subscriber_ids(category)
product_subscriber_ids = provider.list_product_subscriber_ids(product_key)
```

Use event types `system_announcement`, `teaching_announcement`, `policy_published`, and `agriculture_reminder`. All use `event_key=f"{event_type}:{event_id}"`. Policy content contains the title and category. Reminder content contains product and month.

- [ ] **Step 4: Run the broadcast tests**

Run: `uv run --directory backend python -m unittest tests.test_notification_broadcasts -v`

Expected: all broadcast tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/messaging/broadcasts.py backend/tests/test_notification_broadcasts.py
git commit -m "feat(messaging): 实现公告与订阅推送"
```

---

### Task 8: Messaging JSON API and Application Registration

**Files:**
- Create: `backend/app/messaging/routes.py`
- Test: `backend/tests/test_messaging_api.py`
- Modify: `backend/app/__init__.py`

**Interfaces:**
- Consumes: Tasks 2-7 service functions; `load_session()`.
- Produces:
  - `messages_bp: Blueprint`
  - `GET /api/messages/summary`
  - `GET /api/messages/contacts`
  - `GET /api/messages/conversations`
  - `POST /api/messages/conversations`
  - `GET /api/messages/conversations/<int:conversation_id>`
  - `POST /api/messages/conversations/<int:conversation_id>/messages`
  - `GET /api/messages/notifications`
  - `POST /api/messages/private/<int:message_id>/read`
  - `POST /api/messages/notifications/<int:notification_id>/read`
  - `POST /api/messages/read-all`
  - `POST /api/messages/clear-read`

- [ ] **Step 1: Write the failing API tests**

Create `backend/tests/test_messaging_api.py`:

```python
def test_contact_and_conversation_flow(self):
    contacts = self.client.get("/api/messages/contacts")
    self.assertEqual(contacts.status_code, 200)
    self.assertEqual(contacts.get_json()["contacts"][0]["role"], "teacher")

    sent = self.client.post(
        "/api/messages/conversations",
        json={"recipient_id": self.teacher_id, "body": "老师您好"},
    )
    self.assertEqual(sent.status_code, 201)
    conversation_id = sent.get_json()["conversation"]["id"]

    reply = self.client.post(
        f"/api/messages/conversations/{conversation_id}/messages",
        json={"body": "你好"},
    )
    self.assertEqual(reply.status_code, 201)

def test_admin_can_read_notifications_but_cannot_send_private_messages(self):
    self.assertEqual(
        self.client.get("/api/messages/notifications").status_code,
        200,
    )
    denied = self.client.post(
        "/api/messages/conversations",
        json={"recipient_id": self.student_id, "body": "不允许"},
    )
    self.assertEqual(denied.status_code, 403)
```

Also assert `400` invalid body, `401` no active session, `403` prohibited pair, `404` foreign or missing record, `201` send/reply, and `200` for all reads and lists.

- [ ] **Step 2: Run the tests to verify they fail**

Run: `uv run --directory backend python -m unittest tests.test_messaging_api -v`

Expected: FAIL because `/api/messages/*` is not registered.

- [ ] **Step 3: Implement routes and registration**

Create `backend/app/messaging/routes.py`:

```python
messages_bp = Blueprint("messages", __name__, url_prefix="/api/messages")


def _active_session() -> dict:
    return load_session(required=True, allowed_states={"active"})
```

Every route calls `_active_session()` and passes `int(session["id"])` to the service layer. Normalize a non-dict JSON payload to `{}`.

Use these response bodies:

```python
{"success": True, **get_message_summary(user_id)}
{"success": True, "contacts": list_allowed_contacts(user_id)}
{"success": True, "conversations": list_conversations(user_id)}
{"success": True, "conversation": record["conversation"], "message": record["message"]}
{"success": True, "conversation": thread["conversation"], "messages": thread["messages"]}
{"success": True, "notifications": list_notifications(user_id)}
{"success": True, **updated_summary}
```

Map domain errors:

```python
except MessageValidationError as error:
    return jsonify(success=False, errors=error.errors), 400
except MessagingAccessDeniedError:
    return jsonify(success=False, message="当前角色或关系不允许此私信"), 403
except ConversationNotFoundError:
    return jsonify(success=False, message="会话不存在"), 404
except MessageNotFoundError:
    return jsonify(success=False, message="消息不存在"), 404
```

Modify `backend/app/__init__.py`:

```python
from app.messaging.routes import messages_bp

PROTECTED_API_PREFIXES = (
    "/api/student",
    "/api/teacher",
    "/api/enterprise",
    "/api/government",
    "/api/admin",
    "/api/onboarding",
    "/api/messages",
)

app.register_blueprint(messages_bp)
```

Send and reply return `201`; other successful routes return `200`.

- [ ] **Step 4: Run the API tests**

Run: `uv run --directory backend python -m unittest tests.test_messaging_api -v`

Expected: all API tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/messaging/routes.py backend/app/__init__.py backend/tests/test_messaging_api.py
git commit -m "feat(messaging): 提供消息中心接口"
```

---

### Task 9: Frontend Message DTOs, Store, and Global Badge

**Files:**
- Modify: `frontend/src/api/types.ts`
- Create: `frontend/src/stores/messages.ts`
- Test: `frontend/src/stores/messages.test.ts`
- Create: `frontend/src/components/MessageBadge.vue`
- Test: `frontend/src/components/MessageBadge.test.ts`
- Modify: `frontend/src/components/AppHeader.vue`

**Interfaces:**
- Consumes: Task 8 endpoints through `apiFetch()`.
- Produces:
  - `MessagingSummary`
  - `MessageContact`
  - `ConversationSummary`
  - `PrivateMessage`
  - `SystemNotification`
  - `useMessageStore()`
  - `MessageBadge.vue`

- [ ] **Step 1: Write the failing store and badge tests**

Create `frontend/src/stores/messages.test.ts`:

```ts
it('updates the global badge after reading one item', async () => {
  mockedApiFetch
    .mockResolvedValueOnce({
      success: true,
      unread_private: 2,
      unread_notifications: 1,
      unread_total: 3
    })
    .mockResolvedValueOnce({
      success: true,
      unread_private: 1,
      unread_notifications: 1,
      unread_total: 2
    })

  const store = useMessageStore()
  await store.loadSummary()
  await store.markPrivateRead(7)

  expect(store.summary.unread_total).toBe(2)
})
```

Create `frontend/src/components/MessageBadge.test.ts` and assert:

- `12` renders `12`;
- totals above `99` render `99+`;
- `0` renders no numeric bubble;
- link target is `/messages`;
- accessible label is `消息中心，未读 12 条`.

- [ ] **Step 2: Run the tests to verify they fail**

Run: `cd frontend; npm test -- src/stores/messages.test.ts src/components/MessageBadge.test.ts`

Expected: FAIL because the store and component do not exist.

- [ ] **Step 3: Add DTOs, store, badge, and header integration**

Add to `frontend/src/api/types.ts`:

```ts
export interface MessagingSummary {
  unread_private: number
  unread_notifications: number
  unread_total: number
}

export interface MessageContact {
  id: number
  name: string
  role: UserRole
  relationship: 'teacher_student' | 'application'
}

export interface PrivateMessage {
  id: number
  conversation_id: number
  sender_id: number
  body: string
  created_at: string
  read: boolean
}

export interface ConversationSummary {
  id: number
  participant: MessageContact
  last_message: PrivateMessage
  unread_count: number
  updated_at: string
}

export interface SystemNotification {
  id: number
  event_type: string
  title: string
  body: string
  source_type: string | null
  source_id: string | null
  source_available: boolean
  created_at: string
  read: boolean
}
```

Create `frontend/src/stores/messages.ts` with state:

```ts
summary: MessagingSummary
contacts: MessageContact[]
conversations: ConversationSummary[]
activeThreadId: number | null
messages: PrivateMessage[]
notifications: SystemNotification[]
loading: boolean
error: string
```

Create these actions:

```ts
async function loadSummary(): Promise<MessagingSummary>
async function loadContacts(): Promise<MessageContact[]>
async function loadConversations(): Promise<ConversationSummary[]>
async function loadThread(conversationId: number): Promise<PrivateMessage[]>
async function sendMessage(recipientId: number, body: string): Promise<void>
async function reply(conversationId: number, body: string): Promise<void>
async function loadNotifications(): Promise<SystemNotification[]>
async function markPrivateRead(messageId: number): Promise<void>
async function markNotificationRead(notificationId: number): Promise<void>
async function markAllRead(): Promise<void>
async function clearRead(): Promise<void>
```

After send, reply, mark-read, mark-all, or clear-read, refresh the summary and affected list.

Create `MessageBadge.vue` with `Mail`, `RouterLink`, and `useMessageStore()`. On mount call `loadSummary()`. Render `99+` when the total exceeds `99`.

Modify `AppHeader.vue` to render `<MessageBadge />` before the user chip when `userName` exists.

- [ ] **Step 4: Run tests and typecheck**

Run:

```powershell
cd frontend
npm test -- src/stores/messages.test.ts src/components/MessageBadge.test.ts
npx tsc -b --noEmit
```

Expected: tests PASS and TypeScript reports no errors.

- [ ] **Step 5: Commit**

```powershell
git add frontend/src/api/types.ts frontend/src/stores/messages.ts frontend/src/stores/messages.test.ts frontend/src/components/MessageBadge.vue frontend/src/components/MessageBadge.test.ts frontend/src/components/AppHeader.vue
git commit -m "feat(frontend): 增加全局消息角标"
```

---

### Task 10: Message Center UI and Portal Entries

**Files:**
- Create: `frontend/src/views/MessageCenterView.vue`
- Test: `frontend/src/views/MessageCenterView.test.ts`
- Modify: `frontend/src/router/index.ts`
- Modify: `frontend/src/data/portal-guides.ts`

**Interfaces:**
- Consumes: `useMessageStore()`, `AppHeader`, all Task 9 actions.
- Produces:
  - route `/messages` with `meta: { requiresAuth: true }`
  - private and notification tabs
  - contact selector, conversation list, message thread, reply form
  - exact empty state `暂无消息`
  - `全部已读` and `清除已读` actions

- [ ] **Step 1: Write the failing view tests**

Create `frontend/src/views/MessageCenterView.test.ts`:

```ts
it('renders one empty state when there is no visible content', async () => {
  const wrapper = mount(MessageCenterView, {
    global: { plugins: [router] }
  })
  await flushPromises()
  expect(wrapper.text()).toContain('暂无消息')
})

it('marks a system notification read when opened', async () => {
  const store = useMessageStore()
  store.notifications = [notificationFixture]
  const wrapper = mount(MessageCenterView, {
    global: { plugins: [router] }
  })
  await wrapper.get('[data-test="notification-7"]').trigger('click')
  expect(store.markNotificationRead).toHaveBeenCalledWith(7)
})
```

Add tests for summary display, valid contact conversation creation, absence of prohibited contacts, admin/government private-message notice, `全部已读`, `清除已读`, and unread-item retention.

- [ ] **Step 2: Run the tests to verify they fail**

Run: `cd frontend; npm test -- src/views/MessageCenterView.test.ts`

Expected: FAIL because `MessageCenterView.vue` does not exist.

- [ ] **Step 3: Implement the message center**

Create `MessageCenterView.vue` with:

- `AppHeader`;
- summary for `未读私信`, `未读通知`, and `未读总计`;
- segmented tabs `私信` and `通知`;
- a contact select, conversation list, selected thread, per-message unread trigger, and reply form;
- clickable notifications with title, body, and creation time;
- an exact `来源已不可用` label when `source_available=false`;
- `全部已读` and `清除已读` buttons;
- exact empty state `暂无消息`;
- desktop two-column and mobile single-column layouts;
- a message `当前角色不支持私信` with no composer for administrator and government roles.

Do not add attachment, image, voice, recall, edit, or permanent delete controls.

Register in `frontend/src/router/index.ts`:

```ts
{
  path: '/messages',
  name: 'messages',
  component: MessageCenterView,
  meta: { requiresAuth: true }
}
```

Add this entry to every portal definition array in `frontend/src/data/portal-guides.ts`:

```ts
{
  id: `${portalId}-messages`,
  title: '消息中心',
  description: '查看私信、系统通知与未读消息。',
  href: '/messages'
}
```

Keep each existing `steps` array unchanged so first-use guidance remains scoped to the previously approved entries.

- [ ] **Step 4: Run view tests and build**

Run:

```powershell
cd frontend
npm test -- src/views/MessageCenterView.test.ts
npm run build
```

Expected: message-center tests PASS and production build succeeds.

- [ ] **Step 5: Commit**

```powershell
git add frontend/src/views/MessageCenterView.vue frontend/src/views/MessageCenterView.test.ts frontend/src/router/index.ts frontend/src/data/portal-guides.ts
git commit -m "feat(frontend): 实现消息中心"
```

---

### Task 11: Cross-Service Acceptance, Session Recovery, and Browser Verification

**Files:**
- Create: `backend/tests/test_messaging_integration.py`
- Create: `frontend/src/views/MessageCenterSession.test.ts`
- Modify: `frontend/src/views/MessageCenterView.test.ts`

**Interfaces:**
- Consumes: all Task 1-10 interfaces.
- Produces:
  - backend end-to-end relationship, message, notification, read, clear, and cross-session assertions;
  - frontend `/messages` session-expiry recovery;
  - measurable 30-second delivery, 100-action unread, and three-interaction usability assertions;
  - browser evidence for desktop and mobile.

- [ ] **Step 1: Write the failing integration tests**

Create `backend/tests/test_messaging_integration.py` with one flow that:

1. teacher sends to student;
2. student sees one unread and replies;
3. teacher marks the reply read in one client;
4. a second client for the same teacher still sees it read;
5. enterprise and applied student exchange messages;
6. unrelated student-enterprise pair is rejected;
7. review, application, fulfillment, announcement, policy, and agriculture events reach only intended recipients;
8. repeated delivery does not duplicate;
9. clear-read hides only read items;
10. summary equals unread private plus unread notification counts.

Configure source relationships only through an in-test `MessagingSourceProvider`; do not create `job_applications` or subscription rows.

Add the following measurable assertions:

```python
def test_notification_is_visible_within_30_seconds(self):
    started = time.monotonic()
    emit_notification(
        recipient_id=self.student_id,
        event_key="latency:event-1",
        event_type="latency",
        title="测试通知",
        body="立即可见",
    )
    notifications = list_notifications(self.student_id)
    self.assertEqual(len(notifications), 1)
    self.assertLess(time.monotonic() - started, 30)

def test_100_mixed_actions_keep_unread_count_exact(self):
    for index in range(100):
        sent = send_private_message(
            self.teacher_id,
            self.student_id,
            f"消息{index}",
        )
        if index % 2 == 0:
            mark_private_message_read(
                self.student_id,
                sent["message"]["id"],
            )
    expected = 50
    self.assertEqual(
        get_message_summary(self.student_id)["unread_private"],
        expected,
    )
```

Add the badge-link assertion to `frontend/src/components/MessageBadge.test.ts`; it represents interaction one. Add this assertion to `frontend/src/views/MessageCenterView.test.ts`; it represents interactions two and three:

```ts
it('marks an unread item within three interactions', async () => {
  const store = useMessageStore()
  store.messages = [
    {
      id: 7,
      conversation_id: 2,
      sender_id: 1,
      body: '未读消息',
      created_at: '2026-09-15T00:00:00+00:00',
      read: false
    }
  ]
  const wrapper = mount(MessageCenterView, {
    global: { plugins: [pinia, router] }
  })
  await wrapper.get('[data-test="private-tab"]').trigger('click')
  await wrapper.get('[data-test="message-7"]').trigger('click')
  expect(store.markPrivateRead).toHaveBeenCalledWith(7)
})
```

Create `frontend/src/views/MessageCenterSession.test.ts` by following `CourseCatalogSession.test.ts`; assert a `401` from `/api/messages/summary` redirects to `/login?redirect=%2Fmessages`.

- [ ] **Step 2: Run the targeted tests to verify they fail**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_messaging_integration -v
cd frontend
npm test -- src/views/MessageCenterSession.test.ts
```

Expected: FAIL until the seeded relationship and session behavior are complete.

- [ ] **Step 3: Inject a test source provider**

In `backend/tests/test_messaging_integration.py`, define and register a provider after `create_app()`:

```python
class IntegrationSourceProvider(NullMessagingSourceProvider):
    def __init__(self, applications, product_subscribers, policy_subscribers):
        self.applications = set(applications)
        self.product_subscribers = product_subscribers
        self.policy_subscribers = policy_subscribers

    def has_application_relationship(
        self, student_id: int, enterprise_id: int
    ) -> bool:
        return (student_id, enterprise_id) in self.applications

    def list_applied_enterprise_ids(self, student_id: int) -> list[int]:
        return sorted(
            enterprise_id
            for current_student, enterprise_id in self.applications
            if current_student == student_id
        )

    def list_applicant_student_ids(self, enterprise_id: int) -> list[int]:
        return sorted(
            student_id
            for student_id, current_enterprise in self.applications
            if current_enterprise == enterprise_id
        )

    def list_policy_subscriber_ids(self, category: str) -> list[int]:
        return list(self.policy_subscribers.get(category, set()))

    def list_product_subscriber_ids(self, product_key: str) -> list[int]:
        return list(self.product_subscribers.get(product_key, set()))


set_messaging_source_provider(
    self.app,
    IntegrationSourceProvider(
        applications={(self.student_id, self.enterprise_id)},
        product_subscribers={"荔枝": {self.student_id}},
        policy_subscribers={"创业支持": {self.student_id}},
    ),
)
```

The production `create_app()` remains backed by `NullMessagingSourceProvider` until the owning business modules register their provider. Do not seed or persist source-module records from 02.

- [ ] **Step 4: Run all automated gates**

Run:

```powershell
uv run --directory backend python -m unittest discover -s tests -v
cd frontend
npm test
npx tsc -b --noEmit
npm run build
```

Expected: all backend tests, frontend tests, typecheck, and build PASS.

- [ ] **Step 5: Run browser acceptance**

Start the services in separate hidden background sessions:

```powershell
uv run --directory backend python run.py
cd frontend
npm run dev
```

Use `agent-browser` to verify:

1. all six roles show the global badge;
2. teacher and student exchange multiple messages;
3. teacher and student reach three-interaction read completion;
4. prohibited contacts are absent and direct request failure shows the fixed rejection message;
5. opening one unread item reduces the badge by one;
6. `全部已读` reaches zero;
7. `清除已读` hides read items and leaves unread items;
8. logout and a second browser context preserve account-level read state;
9. notification fixtures show the required event content;
10. desktop and 375px mobile screenshots have no overlap, clipped text, or horizontal overflow.

- [ ] **Step 6: Commit**

```powershell
git add backend/tests/test_messaging_integration.py frontend/src/views/MessageCenterSession.test.ts frontend/src/views/MessageCenterView.test.ts
git commit -m "test(messaging): 完成消息通知集成验收"
```

---

## Plan Self-Review

### Spec Coverage

| Spec area | Implementation tasks |
| --- | --- |
| FR-001..FR-012 private messaging, relationship matrix, retained threads | Tasks 1-4, 8, 10-11 |
| FR-013..FR-026 notification events, source references, idempotency | Tasks 1, 4-8, 11 |
| FR-027..FR-034 announcements and subscription delivery | Tasks 1, 7-8, 10-11 |
| FR-035..FR-044 unread badge, single read, mark all, clear read, cross-session persistence | Tasks 1, 3-5, 8-11 |
| FR-045..FR-047 message-type boundary, workspace ownership, no recall/edit | Global Constraints, Tasks 2-3, 8, 10 |
| SC-001..SC-006 relationship, events, latency, audience, badge accuracy | Tasks 2-8, 11 |
| SC-007..SC-012 authorization, order, usability, cross-device state, ownership, clear semantics | Tasks 1-5, 8-11 |
| TDD, review gates, branch/worktree, naming, legacy freeze | Global Constraints and Execution Contract |

### Interface Consistency

- Conversation storage uses `participant_low_id` and `participant_high_id`; API payloads use `participant`.
- Every private message has exactly two `private_message_views` rows.
- Unread totals always mean uncleared rows with `read_at IS NULL`.
- Notification event keys always use `event_type:event_id`.
- Business notices, reminders, announcements, and subscription pushes share `system_notifications`.
- Frontend DTO field names remain snake_case to match backend JSON.
- `/api/messages` is protected by the existing active-session prefix middleware.

### Incomplete-Work Scan

- Every task contains exact files, interfaces, failing tests, implementation rules, verification commands, expected results, and a commit boundary.
- No task depends on an undefined function, route, DTO, table, or helper.
- Modules 08 and 09 are limited to the shared application and subscription read contracts; 02 does not implement their business state changes.

### Approval Gate

This plan is ready for user review. SDD implementation must not begin until the user explicitly approves it.
