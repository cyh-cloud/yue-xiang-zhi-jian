# 01-账户与门户 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 01-账户与门户的完整后端契约与 Vue 学员/角色门户，实现学员和教师注册、六类角色登录与 24 小时会话、兴趣标签、个人资料、首次引导及课程聚合。

**Architecture:** 在全新的 `backend/` 中建立 Flask application factory、SQLite schema、服务层和 JSON 路由；会话使用服务端记录加 HttpOnly cookie，未完成兴趣标签的学员持有 pending session。Vue 3 前端以 Pinia 管理已恢复会话，以路由元数据执行角色隔离，并将注册、登录、资料、课程和首次引导拆成独立视图。

**Tech Stack:** Python 3.12、Flask、sqlite3、uv、unittest、Vue 3、Vite、Pinia、Vue Router、Vitest、Vue Test Utils。

**Spec:** `specs/001-user-portal/spec.md`

## Global Constraints

- 自助注册只允许 `student` 和 `teacher`；企业、政府、超级管理员、普通管理员不得自助注册。
- 用户名先 trim，格式为 3-32 位、字母开头、仅字母/数字/下划线；密码不 trim，至少 8 位。
- 用户名冲突固定提示“用户名已被占用”；凭证错误固定提示“用户名或密码错误”；禁用账户固定提示“账户已被禁用，请联系管理员”；忘记密码固定提示“请联系管理员重置密码”。
- 新学员个人资料学习方向默认“综合”；方向只允许 `agriculture`、`ecommerce`、`handcraft`、`comprehensive`。
- 默认会话有效期 24 小时，必须可配置；pending 注册会话不得访问受保护门户。
- 兴趣标签只来自预置目录，分为作物类目、技能兴趣、岗位类别；允许全跳过。
- 首次引导完成或跳过后永久记录；关闭、刷新或离开不得提交完成状态。
- 课程聚合只读取 `published` 课程，方向为农业/电商/手工；兴趣命中优先，其次 `published_at DESC`，最后 `id DESC`；空态固定“暂无课程”。
- 修改学习方向或标签后，下游推荐、统计和 AI 学情报告读取最新值；本 feature 不实现下游计算。
- 旧系统 `app.py`、`database.py`、旧 HTML 页面不可读取、导入、复制或迁移。
- 后端完全置于 `backend/`；依赖以 `backend/pyproject.toml` 和 `backend/uv.lock` 为权威。
- 路由 kebab-case、Python snake_case、前端 camelCase；提交信息使用中文短句。
- 每个实现任务执行 TDD：先写失败测试、确认失败、最小实现、确认通过、提交。
- 每个任务由独立 reviewer 做规格合规与代码质量双维审查；Critical/Important 未关闭不得进入下一任务。

## Execution Contract

- 实现者：`基元律动/deepseek-flash`，effort `max`。
- 任务级审查：`基元律动/glm-5.3`，effort `high`。
- 修复 4-5 轮：fresh implementer，`基元律动/glm-5.3`，effort `high`。
- 最终全分支审查：`基元律动/glm-5.3`，effort `max`。
- 每次 spawn 必须显式传 `model` 和 `effort`；每个 implementer 不得再 spawn 子代理。
- implementer 必须提交真实 diff、真实测试命令和真实输出；reviewer 必须逐项核对“真实代码、真实验证、真实接口对齐”，禁止以自述代替证据。
- 任务间不请求人工确认；只有不可逆操作、安全敏感动作、worktree 外副作用、计划破碎到每一步都只能猜时才停机。
- worktree 使用 `.worktrees/001-user-portal` 并挂当前分支 `v2/lixKRT/001-user-portal`；不得另开分支。
- SDD ledger：`.superpowers/sdd/2026-09-14-001-user-portal.md/progress.md`。

## File Structure

| Path | Responsibility |
| --- | --- |
| `backend/pyproject.toml`, `backend/uv.lock` | Backend dependency authority |
| `backend/run.py` | Local Flask launcher |
| `backend/app/__init__.py` | Application factory and route registration |
| `backend/app/config.py` | Runtime configuration |
| `backend/app/db.py` | SQLite connection, schema, transaction helpers |
| `backend/app/session_manager.py` | Server-side session issue/load/revoke and auth decorators |
| `backend/app/auth/validators.py` | Registration validation and exact messages |
| `backend/app/auth/service.py` | Register, login, pending enrollment, route target |
| `backend/app/auth/routes.py` | `/api/auth/*` routes |
| `backend/app/tags/service.py` | Interest-tag catalog and student selection |
| `backend/app/tags/routes.py` | `/api/interest-tags` |
| `backend/app/profiles/service.py` | Student profile and downstream preference contract |
| `backend/app/profiles/routes.py` | `/api/student/profile` |
| `backend/app/onboarding/service.py` | Per-role guidance state |
| `backend/app/onboarding/routes.py` | `/api/onboarding/*` |
| `backend/app/courses/service.py` | Published course aggregation and recommendation ordering |
| `backend/app/courses/routes.py` | `/api/student/courses` |
| `backend/app/seed.py` | Idempotent local-only catalog/demo data seeding |
| `backend/tests/` | Backend unit/integration tests |
| `frontend/src/api/types.ts` | Shared API DTOs and exact union types |
| `frontend/src/api/client.ts` | Cookie-aware fetch and typed errors |
| `frontend/src/stores/auth.ts` | Session restoration, login, registration, logout |
| `frontend/src/stores/student.ts` | Profile, tags, onboarding and courses state |
| `frontend/src/router/roleRoutes.ts` | Pure role-to-default-path mapping |
| `frontend/src/router/index.ts` | Routes and auth/role guards |
| `frontend/src/views/LoginView.vue` | Login page |
| `frontend/src/views/RegisterView.vue` | Student/teacher registration page |
| `frontend/src/views/InterestTagsView.vue` | Post-registration tag selection/skip |
| `frontend/src/components/OnboardingOverlay.vue` | First-use step overlay |
| `frontend/src/components/PortalShell.vue` | Shared role-portal shell |
| `frontend/src/views/StudentPortalView.vue` | Student portal entry |
| `frontend/src/views/TeacherPortalView.vue` | Teacher portal entry |
| `frontend/src/views/EnterprisePortalView.vue` | Enterprise portal entry |
| `frontend/src/views/GovernmentPortalView.vue` | Government portal entry |
| `frontend/src/views/AdminPortalView.vue` | Both administrator roles’ backend entry |
| `frontend/src/views/StudentProfileView.vue` | Student profile editing |
| `frontend/src/views/CourseCatalogView.vue` | Course aggregation |
| `frontend/src/data/portal-guides.ts` | Exact first-use steps per portal |

---

### Task 1: Backend Foundation, Schema, and Health Contract

**Files:**
- Create: `backend/pyproject.toml`
- Create: `backend/run.py`
- Create: `backend/app/__init__.py`
- Create: `backend/app/config.py`
- Create: `backend/app/db.py`
- Create: `backend/tests/__init__.py`
- Create: `backend/tests/test_foundation.py`

**Interfaces:**
- Consumes: None.
- Produces: `create_app(test_config: dict | None = None) -> Flask`; `get_db() -> sqlite3.Connection`; `init_db(connection: sqlite3.Connection) -> None`; `close_db(error: BaseException | None = None) -> None`.

- [ ] **Step 1: Write the failing foundation test**

Create `backend/tests/test_foundation.py`:

```python
import tempfile
import unittest
from pathlib import Path

from app import create_app
from app.db import get_db


class TestFoundation(unittest.TestCase):
    def setUp(self):
        self.temp_dir = tempfile.TemporaryDirectory()
        self.app = create_app(
            {
                "TESTING": True,
                "DATABASE_PATH": str(Path(self.temp_dir.name) / "test.db"),
                "SECRET_KEY": "test-only-secret",
            }
        )
        self.client = self.app.test_client()

    def tearDown(self):
        self.temp_dir.cleanup()

    def test_health_endpoint_returns_success(self):
        response = self.client.get("/api/health")

        self.assertEqual(response.status_code, 200)
        self.assertEqual(response.get_json(), {"success": True, "status": "ok"})

    def test_schema_contains_core_tables(self):
        with self.app.app_context():
            names = {
                row["name"]
                for row in get_db().execute(
                    "SELECT name FROM sqlite_master WHERE type = 'table'"
                )
            }

        expected = {
            "users",
            "sessions",
            "student_profiles",
            "interest_tags",
            "student_interest_tags",
            "onboarding_states",
            "resumes",
            "courses",
            "course_interest_tags",
        }
        self.assertTrue(expected.issubset(names))
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_foundation -v
```

Expected: FAIL because `backend/pyproject.toml` or `app` does not exist.

- [ ] **Step 3: Create the backend project and app factory**

Create `backend/pyproject.toml`:

```toml
[project]
name = "yuexiang-artisan-backend"
version = "0.1.0"
description = "YueXiang artisan platform v2 backend"
requires-python = ">=3.12,<3.14"
dependencies = [
  "flask>=3.1,<4",
  "python-dotenv>=1.0,<2",
]

[tool.uv]
package = false
```

Create `backend/run.py`:

```python
from app import create_app


app = create_app()


if __name__ == "__main__":
    app.run(host="127.0.0.1", port=5000, debug=True)
```

Create `backend/app/config.py`:

```python
from __future__ import annotations

import os
from pathlib import Path


BACKEND_ROOT = Path(__file__).resolve().parents[1]


def build_config() -> dict[str, object]:
    return {
        "SECRET_KEY": os.environ.get("SECRET_KEY", "dev-only-change-me"),
        "DATABASE_PATH": os.environ.get(
            "DATABASE_PATH", str(BACKEND_ROOT.parent / "data" / "yuexiang.db")
        ),
        "SESSION_HOURS": int(os.environ.get("SESSION_HOURS", "24")),
        "SESSION_COOKIE_SECURE": os.environ.get("SESSION_COOKIE_SECURE") == "1",
        "MAX_CONTENT_LENGTH": 2 * 1024 * 1024,
    }
```

Create `backend/app/__init__.py`:

```python
from __future__ import annotations

from flask import Flask, jsonify

from app.config import build_config
from app.db import close_db, init_db


def create_app(test_config: dict | None = None) -> Flask:
    app = Flask(__name__)
    app.config.from_mapping(build_config())
    if test_config:
        app.config.update(test_config)

    app.teardown_appcontext(close_db)

    @app.get("/api/health")
    def health():
        return jsonify(success=True, status="ok")

    with app.app_context():
        init_db()

    return app
```

Create `backend/app/db.py` with the complete schema below:

```python
from __future__ import annotations

import sqlite3
from pathlib import Path

from flask import current_app, g


SCHEMA_SQL = """
PRAGMA foreign_keys = ON;

CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL UNIQUE,
    password_hash TEXT NOT NULL,
    name TEXT NOT NULL,
    role TEXT NOT NULL CHECK (
        role IN ('student', 'teacher', 'enterprise', 'government', 'super_admin', 'admin')
    ),
    is_enabled INTEGER NOT NULL DEFAULT 1 CHECK (is_enabled IN (0, 1)),
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS sessions (
    token_hash TEXT PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    state TEXT NOT NULL CHECK (state IN ('pending', 'active')),
    expires_at TEXT NOT NULL,
    created_at TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_sessions_user_id ON sessions(user_id);

CREATE TABLE IF NOT EXISTS student_profiles (
    user_id INTEGER PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
    contact TEXT NOT NULL DEFAULT '',
    learning_direction TEXT NOT NULL DEFAULT 'comprehensive' CHECK (
        learning_direction IN ('agriculture', 'ecommerce', 'handcraft', 'comprehensive')
    ),
    updated_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS interest_tags (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    group_key TEXT NOT NULL CHECK (group_key IN ('crop', 'skill', 'job')),
    name TEXT NOT NULL,
    sort_order INTEGER NOT NULL DEFAULT 0,
    is_active INTEGER NOT NULL DEFAULT 1 CHECK (is_active IN (0, 1)),
    UNIQUE (group_key, name)
);

CREATE TABLE IF NOT EXISTS student_interest_tags (
    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    tag_id INTEGER NOT NULL REFERENCES interest_tags(id) ON DELETE CASCADE,
    PRIMARY KEY (user_id, tag_id)
);

CREATE TABLE IF NOT EXISTS onboarding_states (
    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    portal TEXT NOT NULL CHECK (
        portal IN ('student', 'teacher', 'enterprise', 'government', 'admin')
    ),
    outcome TEXT NOT NULL CHECK (outcome IN ('completed', 'skipped')),
    completed_at TEXT NOT NULL,
    PRIMARY KEY (user_id, portal)
);

CREATE TABLE IF NOT EXISTS resumes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL UNIQUE REFERENCES users(id) ON DELETE CASCADE,
    created_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS courses (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    direction TEXT NOT NULL CHECK (
        direction IN ('agriculture', 'ecommerce', 'handcraft')
    ),
    status TEXT NOT NULL CHECK (status IN ('draft', 'pending', 'published', 'offline')),
    published_at TEXT,
    summary TEXT NOT NULL DEFAULT '',
    teacher_name TEXT NOT NULL DEFAULT '',
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_courses_catalog
    ON courses(status, direction, published_at DESC, id DESC);

CREATE TABLE IF NOT EXISTS course_interest_tags (
    course_id INTEGER NOT NULL REFERENCES courses(id) ON DELETE CASCADE,
    tag_id INTEGER NOT NULL REFERENCES interest_tags(id) ON DELETE CASCADE,
    PRIMARY KEY (course_id, tag_id)
);
"""


def get_db() -> sqlite3.Connection:
    if "db" not in g:
        database_path = Path(current_app.config["DATABASE_PATH"])
        database_path.parent.mkdir(parents=True, exist_ok=True)
        connection = sqlite3.connect(database_path)
        connection.row_factory = sqlite3.Row
        connection.execute("PRAGMA foreign_keys = ON")
        g.db = connection
    return g.db


def init_db(connection: sqlite3.Connection | None = None) -> None:
    db = connection or get_db()
    db.executescript(SCHEMA_SQL)
    db.commit()


def close_db(error: BaseException | None = None) -> None:
    db = g.pop("db", None)
    if db is not None:
        db.close()
```

- [ ] **Step 4: Lock dependencies and run foundation tests**

Run:

```powershell
uv lock --directory backend
uv run --directory backend python -m unittest tests.test_foundation -v
```

Expected: 2 tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/pyproject.toml backend/uv.lock backend/run.py backend/app backend/tests
git commit -m "feat(backend): 建立 v2 后端与数据库基础"
```

---

### Task 2: Student and Teacher Registration

**Files:**
- Create: `backend/app/auth/__init__.py`
- Create: `backend/app/auth/validators.py`
- Create: `backend/app/auth/service.py`
- Create: `backend/app/auth/routes.py`
- Create: `backend/app/session_manager.py`
- Create: `backend/tests/test_registration.py`
- Modify: `backend/app/__init__.py`

**Interfaces:**
- Consumes: `get_db()` from Task 1.
- Produces: `validate_registration(payload: dict) -> dict[str, str]`; `register_account(payload: dict, session_hours: int) -> dict`; `issue_session(user_id: int, state: str, session_hours: int) -> str`; `apply_session_cookie(response: Response, token: str, max_age: int) -> Response`; `POST /api/auth/register` returning `success`, `next_step`, `user`.

- [ ] **Step 1: Write failing registration tests**

Create table-driven tests covering every branch:

```python
class TestRegistration(unittest.TestCase):
    def test_student_registration_creates_default_profile(self):
        response = self.client.post(
            "/api/auth/register",
            json={
                "role": "student",
                "username": "student01",
                "password": "password8",
                "confirm_password": "password8",
                "name": "林晓",
            },
        )

        self.assertEqual(response.status_code, 201)
        self.assertEqual(response.get_json()["next_step"], "interest-tags")
        with self.app.app_context():
            profile = get_db().execute(
                "SELECT learning_direction FROM student_profiles WHERE user_id = 1"
            ).fetchone()
            resume = get_db().execute(
                "SELECT user_id FROM resumes WHERE user_id = 1"
            ).fetchone()
        self.assertEqual(profile["learning_direction"], "comprehensive")
        self.assertEqual(resume["user_id"], 1)

    def test_registration_validation_errors_are_field_specific(self):
        cases = (
            ({"username": ""}, "username", "用户名不能为空"),
            ({"username": "1bad"}, "username", "用户名格式不正确"),
            ({"password": ""}, "password", "密码不能为空"),
            ({"password": "short"}, "password", "密码长度不能少于8位"),
            ({"confirm_password": "different8"}, "confirm_password", "两次输入的密码不一致"),
            ({"name": ""}, "name", "姓名不能为空"),
        )
        base = {
            "role": "student",
            "username": "student01",
            "password": "password8",
            "confirm_password": "password8",
            "name": "林晓",
        }
        for patch, field, message in cases:
            with self.subTest(field=field):
                response = self.client.post("/api/auth/register", json={**base, **patch})
                self.assertEqual(response.status_code, 400)
                self.assertEqual(response.get_json()["errors"][field], message)

    def test_duplicate_username_is_rejected_for_any_role(self):
        first = {
            "role": "teacher",
            "username": "shared01",
            "password": "password8",
            "confirm_password": "password8",
            "name": "教师甲",
        }
        self.client.post("/api/auth/register", json=first)
        response = self.client.post(
            "/api/auth/register", json={**first, "role": "student", "name": "学员乙"}
        )

        self.assertEqual(response.status_code, 409)
        self.assertEqual(response.get_json()["errors"]["username"], "用户名已被占用")

    def test_other_roles_cannot_self_register(self):
        response = self.client.post(
            "/api/auth/register",
            json={
                "role": "enterprise",
                "username": "company01",
                "password": "password8",
                "confirm_password": "password8",
                "name": "企业甲",
            },
        )

        self.assertEqual(response.status_code, 400)
        self.assertEqual(response.get_json()["errors"]["role"], "该角色不支持自助注册")
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_registration -v
```

Expected: FAIL with HTTP 404.

- [ ] **Step 3: Implement validation, persistence, and pending enrollment**

Use these exact constants and functions in `validators.py`:

```python
USERNAME_PATTERN = re.compile(r"^[A-Za-z][A-Za-z0-9_]{2,31}$")
SELF_REGISTER_ROLES = {"student", "teacher"}


def validate_registration(payload: dict) -> dict[str, str]:
    errors: dict[str, str] = {}
    role = payload.get("role")
    username = str(payload.get("username", "")).strip()
    password = str(payload.get("password", ""))
    confirmation = str(payload.get("confirm_password", ""))
    name = str(payload.get("name", "")).strip()

    if role not in SELF_REGISTER_ROLES:
        errors["role"] = "该角色不支持自助注册"
    if not username:
        errors["username"] = "用户名不能为空"
    elif not USERNAME_PATTERN.fullmatch(username):
        errors["username"] = "用户名格式不正确"
    if not password:
        errors["password"] = "密码不能为空"
    elif len(password) < 8:
        errors["password"] = "密码长度不能少于8位"
    if confirmation != password:
        errors["confirm_password"] = "两次输入的密码不一致"
    if not name:
        errors["name"] = "姓名不能为空"
    return errors
```

`register_account()` must:

- Normalize username and name by trimming.
- Hash the password with `generate_password_hash`.
- Insert `users`, `student_profiles` for students, and `resumes` for students in one transaction.
- Create a `sessions` row with `state='pending'` for students and `state='active'` for teachers.
- Set the raw session token in cookie `yx_session`; store only its SHA-256 hash.
- Return `next_step='interest-tags'` for students and `next_step='portal'` for teachers.
- Return HTTP 409 for `sqlite3.IntegrityError` and field error `{"username": "用户名已被占用"}`.

Register the blueprint from `create_app()`:

```python
from app.auth.routes import auth_bp
app.register_blueprint(auth_bp)
```

- [ ] **Step 4: Run registration tests**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_registration -v
```

Expected: All registration tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/__init__.py backend/app/auth backend/app/session_manager.py backend/tests/test_registration.py
git commit -m "feat(auth): 实现学员与教师注册"
```

---

### Task 3: Login, Session Validation, Logout, and Role Routing

**Files:**
- Create: `backend/tests/test_login_session.py`
- Modify: `backend/app/session_manager.py`
- Modify: `backend/app/auth/routes.py`
- Modify: `backend/app/auth/service.py`
- Modify: `backend/app/__init__.py`

**Interfaces:**
- Consumes: `get_db()` from Task 1; `register_account()` and `issue_session()` from Task 2.
- Produces: `load_session(required: bool = True, allowed_states: set[str] | None = None) -> dict | None`; `clear_session_cookie(response: Response) -> Response`; `role_default_path(role: str) -> str`; `authenticate(username: str, password: str, session_hours: int) -> dict | None`.

- [ ] **Step 1: Write failing login/session tests**

Test these exact outcomes:

```python
def test_all_roles_route_to_expected_portal(self):
    cases = {
        "student": "/student",
        "teacher": "/teacher",
        "enterprise": "/enterprise",
        "government": "/government",
        "super_admin": "/admin",
        "admin": "/admin",
    }
    for role, expected in cases.items():
        with self.subTest(role=role):
            user_id = self.create_user(role=role, username=f"{role}01")
            response = self.client.post(
                "/api/auth/login",
                json={"username": f"{role}01", "password": "password8"},
            )
            self.assertEqual(response.status_code, 200)
            self.assertEqual(response.get_json()["default_path"], expected)

def test_unknown_username_and_wrong_password_share_message(self):
    responses = [
        self.client.post("/api/auth/login", json={"username": "missing", "password": "password8"}),
        self.client.post("/api/auth/login", json={"username": "student01", "password": "wrongpass"}),
    ]
    for response in responses:
        self.assertEqual(response.status_code, 401)
        self.assertEqual(response.get_json()["message"], "用户名或密码错误")

def test_disabled_account_has_distinct_message(self):
    response = self.client.post(
        "/api/auth/login", json={"username": "disabled01", "password": "password8"}
    )
    self.assertEqual(response.status_code, 403)
    self.assertEqual(response.get_json()["message"], "账户已被禁用，请联系管理员")

def test_expired_session_redirects_with_original_target(self):
    response = self.client.get("/api/student/profile?next=/student/courses")
    self.assertEqual(response.status_code, 401)
    self.assertEqual(response.get_json()["redirect"], "/login?redirect=%2Fstudent%2Fcourses")
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_login_session -v
```

Expected: FAIL because `/api/auth/login` and session checks do not exist.

- [ ] **Step 3: Implement session manager and login routes**

Use SHA-256 hashing and `secrets.token_urlsafe(32)`. Validate on every protected request:

```python
def load_session(
    required: bool = True, allowed_states: set[str] | None = None
) -> dict | None:
    raw_token = request.cookies.get("yx_session")
    if not raw_token:
        if required:
            abort_session_required()
        return None

    token_hash = hashlib.sha256(raw_token.encode("utf-8")).hexdigest()
    row = get_db().execute(
        """
        SELECT s.token_hash, s.state, s.expires_at, u.*
        FROM sessions s
        JOIN users u ON u.id = s.user_id
        WHERE s.token_hash = ?
        """,
        (token_hash,),
    ).fetchone()
    if row is None or row["expires_at"] <= utc_now_iso():
        if required:
            abort_session_required()
        return None
    if not row["is_enabled"]:
        get_db().execute("DELETE FROM sessions WHERE token_hash = ?", (token_hash,))
        get_db().commit()
        abort_disabled_account()
    if allowed_states and row["state"] not in allowed_states:
        if required:
            abort_session_required()
        return None
    return dict(row)
```

Add:

- `POST /api/auth/login`: validate credentials, reject disabled accounts, revoke prior cookie token if present, issue active session, return `user` and `default_path`.
- `POST /api/auth/logout`: delete current session row, clear cookie, always return `{"success": True}`.
- `GET /api/auth/session`: return active session user or a recoverable pending student state with `state='pending'`, `next_step='interest-tags'` and `default_path='/register/interest-tags'`; return HTTP 401 with `redirect` when no valid session exists.
- Cookie flags: `httponly=True`, `samesite="Lax"`, `secure=app.config["SESSION_COOKIE_SECURE"]`, `max_age=SESSION_HOURS * 3600`.

- [ ] **Step 4: Run login/session tests**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_login_session -v
```

Expected: All login/session tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/session_manager.py backend/app/auth backend/app/__init__.py backend/tests/test_login_session.py
git commit -m "feat(auth): 增加登录会话与角色路由"
```

---

### Task 4: Interest-Tag Catalog and Pending Enrollment Completion

**Files:**
- Create: `backend/app/seed.py`
- Create: `backend/app/tags/__init__.py`
- Create: `backend/app/tags/service.py`
- Create: `backend/app/tags/routes.py`
- Create: `backend/tests/test_interest_tags.py`
- Modify: `backend/app/__init__.py`
- Modify: `backend/app/db.py`
- Modify: `backend/app/auth/routes.py`

**Interfaces:**
- Consumes: `load_session()` from Task 3; registration pending session from Task 2.
- Produces: `seed_interest_tags(connection: sqlite3.Connection) -> None`; `list_interest_tags() -> list[dict]`; `replace_student_tags(user_id: int, tag_ids: list[int]) -> None`; `GET /api/interest-tags`; `POST /api/auth/interest-tags`.

- [ ] **Step 1: Write failing tag and enrollment tests**

```python
def test_tag_catalog_has_three_exact_groups(self):
    response = self.client.get("/api/interest-tags")
    self.assertEqual(response.status_code, 200)
    groups = {item["group_key"] for item in response.get_json()["tags"]}
    self.assertEqual(groups, {"crop", "skill", "job"})

def test_student_can_save_tags_then_session_becomes_active(self):
    self.register_student("student01")
    tag_ids = [tag["id"] for tag in self.client.get("/api/interest-tags").get_json()["tags"][:2]]
    saved = self.client.post("/api/auth/interest-tags", json={"tag_ids": tag_ids})
    self.assertEqual(saved.status_code, 200)
    self.assertEqual(saved.get_json()["default_path"], "/student")

    session = self.client.get("/api/auth/session")
    self.assertEqual(session.status_code, 200)
    self.assertEqual(session.get_json()["user"]["role"], "student")

def test_student_can_skip_all_tags(self):
    self.register_student("student01")
    response = self.client.post("/api/auth/interest-tags", json={"tag_ids": []})
    self.assertEqual(response.status_code, 200)
    with self.app.app_context():
        count = get_db().execute(
            "SELECT COUNT(*) AS total FROM student_interest_tags"
        ).fetchone()["total"]
    self.assertEqual(count, 0)

def test_pending_session_cannot_open_student_profile(self):
    self.register_student("student01")
    response = self.client.get("/api/student/profile")
    self.assertEqual(response.status_code, 401)
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_interest_tags -v
```

Expected: FAIL because tag routes and completion route do not exist.

- [ ] **Step 3: Implement catalog, replacement, and session activation**

Seed this exact catalog in `backend/app/seed.py` and invoke the idempotent seed from `init_db()`:

```python
DEFAULT_INTEREST_TAGS = (
    ("crop", "荔枝"),
    ("crop", "龙眼"),
    ("crop", "水稻"),
    ("crop", "水产"),
    ("skill", "电商直播"),
    ("skill", "短视频"),
    ("skill", "客服沟通"),
    ("skill", "手工艺"),
    ("job", "农业技术员"),
    ("job", "电商运营"),
    ("job", "客服专员"),
    ("job", "手工艺人"),
)
```

`replace_student_tags()` must:

- Accept only IDs that exist and are active.
- Replace the student’s full tag set in one transaction.
- Raise a 400 field error for unknown or inactive IDs.

`POST /api/auth/interest-tags` must:

- Require a pending session for role `student`.
- Save the submitted set, including an empty set.
- Change that session row to `state='active'`.
- Return `success`, `user`, and `default_path='/student'`.

- [ ] **Step 4: Run tag tests**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_interest_tags -v
```

Expected: All tag and enrollment tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/seed.py backend/app/tags backend/app/auth/routes.py backend/app/__init__.py backend/tests/test_interest_tags.py
git commit -m "feat(profile): 增加兴趣标签引导"
```

---

### Task 5: Student Profile and Downstream Preference Contract

**Files:**
- Create: `backend/app/profiles/__init__.py`
- Create: `backend/app/profiles/service.py`
- Create: `backend/app/profiles/routes.py`
- Create: `backend/tests/test_student_profile.py`
- Modify: `backend/app/__init__.py`

**Interfaces:**
- Consumes: active-session validation from Task 3; tag relations from Task 4.
- Produces: `get_student_profile(user_id: int) -> dict`; `update_student_profile(user_id: int, payload: dict) -> dict`; `get_profile_preferences(user_id: int) -> dict`; `GET /api/student/profile`; `PUT /api/student/profile`.

- [ ] **Step 1: Write failing profile tests**

```python
def test_profile_returns_default_direction_and_selected_tags(self):
    self.login_student("student01")
    response = self.client.get("/api/student/profile")
    self.assertEqual(response.status_code, 200)
    self.assertEqual(response.get_json()["profile"]["learning_direction"], "comprehensive")

def test_profile_update_persists_allowed_fields(self):
    self.login_student("student01")
    response = self.client.put(
        "/api/student/profile",
        json={
            "name": "陈晓",
            "contact": "13800000000",
            "learning_direction": "ecommerce",
            "tag_ids": [5, 6],
        },
    )
    self.assertEqual(response.status_code, 200)
    self.assertEqual(response.get_json()["profile"]["learning_direction"], "ecommerce")

def test_downstream_contract_reads_latest_preferences(self):
    self.login_student("student01")
    with self.app.app_context():
        update_student_profile(
            1,
            {
                "name": "陈晓",
                "contact": "",
                "learning_direction": "handcraft",
                "tag_ids": [8],
            },
        )
        preferences = get_profile_preferences(1)
    self.assertEqual(preferences["learning_direction"], "handcraft")
    self.assertEqual(preferences["interest_tag_ids"], [8])

def test_profile_rejects_unknown_direction(self):
    self.login_student("student01")
    response = self.client.put(
        "/api/student/profile",
        json={"name": "陈晓", "contact": "", "learning_direction": "unknown", "tag_ids": []},
    )
    self.assertEqual(response.status_code, 400)
    self.assertIn("learning_direction", response.get_json()["errors"])
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_student_profile -v
```

Expected: FAIL because profile routes do not exist.

- [ ] **Step 3: Implement profile service and API**

Allowed directions:

```python
LEARNING_DIRECTIONS = {"agriculture", "ecommerce", "handcraft", "comprehensive"}
```

`update_student_profile()` must:

- Trim `name` and `contact`.
- Reject empty name and unknown learning direction.
- Update `users.name`, `student_profiles.contact`, and `student_profiles.learning_direction`.
- Replace tags using the Task 4 service.
- Return the same shape as `get_student_profile()`.

`get_profile_preferences()` is the stable downstream contract:

```python
{
    "user_id": 1,
    "learning_direction": "ecommerce",
    "interest_tag_ids": [5, 6],
    "interest_tag_names": ["电商直播", "短视频"],
}
```

- [ ] **Step 4: Run profile tests**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_student_profile -v
```

Expected: All profile tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/profiles backend/app/__init__.py backend/tests/test_student_profile.py
git commit -m "feat(profile): 实现学员资料与偏好同步"
```

---

### Task 6: Per-Role First-Use Guidance State

**Files:**
- Create: `backend/app/onboarding/__init__.py`
- Create: `backend/app/onboarding/service.py`
- Create: `backend/app/onboarding/routes.py`
- Create: `backend/tests/test_onboarding.py`
- Modify: `backend/app/__init__.py`

**Interfaces:**
- Consumes: active-session validation from Task 3.
- Produces: `needs_onboarding(user_id: int, portal: str) -> bool`; `complete_onboarding(user_id: int, portal: str, outcome: str) -> None`; `GET /api/onboarding/<portal>`; `POST /api/onboarding/<portal>/complete`.

- [ ] **Step 1: Write failing onboarding tests**

```python
def test_first_login_requires_onboarding(self):
    self.login_user("teacher01", "teacher")
    response = self.client.get("/api/onboarding/teacher")
    self.assertEqual(response.status_code, 200)
    self.assertTrue(response.get_json()["required"])

def test_complete_or_skip_suppresses_future_prompt(self):
    self.login_user("teacher01", "teacher")
    response = self.client.post(
        "/api/onboarding/teacher/complete", json={"outcome": "skipped"}
    )
    self.assertEqual(response.status_code, 200)
    self.assertFalse(self.client.get("/api/onboarding/teacher").get_json()["required"])

def test_interrupted_guidance_has_no_state_and_returns_next_time(self):
    self.login_user("student01", "student")
    self.assertEqual(self.client.get("/api/onboarding/student").get_json()["required"], True)
    self.assertEqual(self.client.get("/api/onboarding/student").get_json()["required"], True)

def test_role_portal_state_is_independent(self):
    self.login_user("teacher01", "teacher")
    self.client.post("/api/onboarding/teacher/complete", json={"outcome": "completed"})
    response = self.client.get("/api/onboarding/student")
    self.assertEqual(response.status_code, 403)
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_onboarding -v
```

Expected: FAIL because onboarding routes do not exist.

- [ ] **Step 3: Implement guidance state**

- Map roles to portals exactly: student→student, teacher→teacher, enterprise→enterprise, government→government, both admin roles→admin.
- `GET` returns `{"success": True, "required": bool, "portal": portal}`.
- `POST` accepts only `completed` or `skipped`.
- Insert or replace an `onboarding_states` row.
- Only an explicit `POST` records completion; absence of a request keeps the guide required.

- [ ] **Step 4: Run onboarding tests**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_onboarding -v
```

Expected: All onboarding tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/onboarding backend/app/__init__.py backend/tests/test_onboarding.py
git commit -m "feat(portal): 增加首次使用引导状态"
```

---

### Task 7: Published Course Aggregation and Recommendation Ordering

**Files:**
- Create: `backend/app/courses/__init__.py`
- Create: `backend/app/courses/service.py`
- Create: `backend/app/courses/routes.py`
- Create: `backend/tests/test_course_catalog.py`
- Modify: `backend/app/__init__.py`

**Interfaces:**
- Consumes: active student session from Task 3; `get_profile_preferences()` from Task 5; shared `courses` and `course_interest_tags` tables from Task 1.
- Produces: `list_published_courses(user_id: int, direction: str) -> list[dict]`; `GET /api/student/courses?direction=<direction>`.

- [ ] **Step 1: Write failing course tests**

```python
def test_only_published_courses_for_selected_direction_are_returned(self):
    self.seed_course("published", "agriculture", "2026-09-01T00:00:00+00:00")
    self.seed_course("pending", "agriculture", "2026-09-02T00:00:00+00:00")
    self.seed_course("offline", "agriculture", "2026-09-03T00:00:00+00:00")
    self.seed_course("published", "ecommerce", "2026-09-04T00:00:00+00:00")
    self.login_student("student01")

    response = self.client.get("/api/student/courses?direction=agriculture")
    titles = [item["title"] for item in response.get_json()["courses"]]
    self.assertEqual(titles, ["agriculture-published"])

def test_interest_match_ranks_before_newer_nonmatch(self):
    self.seed_course(
        "published", "agriculture", "2026-09-01T00:00:00+00:00", tags=[1]
    )
    self.seed_course(
        "published", "agriculture", "2026-09-12T00:00:00+00:00", tags=[2]
    )
    self.login_student("student01", tag_ids=[1])

    response = self.client.get("/api/student/courses?direction=agriculture")
    self.assertEqual(response.get_json()["courses"][0]["interest_match"], True)

def test_equal_recommendation_level_uses_published_at_desc_then_id_desc(self):
    self.seed_course("published", "agriculture", "2026-09-01T00:00:00+00:00")
    self.seed_course("published", "agriculture", "2026-09-12T00:00:00+00:00")
    self.login_student("student01")

    response = self.client.get("/api/student/courses?direction=agriculture")
    self.assertEqual(
        [item["published_at"] for item in response.get_json()["courses"]],
        ["2026-09-12T00:00:00+00:00", "2026-09-01T00:00:00+00:00"],
    )

def test_empty_direction_returns_empty_list_for_frontend_empty_state(self):
    self.login_student("student01")
    response = self.client.get("/api/student/courses?direction=handcraft")
    self.assertEqual(response.get_json()["courses"], [])
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_course_catalog -v
```

Expected: FAIL because the course route does not exist.

- [ ] **Step 3: Implement the query**

Use one query with an explicit boolean match and deterministic ordering:

```sql
SELECT
    c.id,
    c.title,
    c.direction,
    c.summary,
    c.teacher_name,
    c.published_at,
    CASE WHEN EXISTS (
        SELECT 1
        FROM course_interest_tags cit
        JOIN student_interest_tags sit
          ON sit.tag_id = cit.tag_id
        WHERE cit.course_id = c.id
          AND sit.user_id = ?
    ) THEN 1 ELSE 0 END AS interest_match
FROM courses c
WHERE c.status = 'published'
  AND c.direction = ?
ORDER BY interest_match DESC, c.published_at DESC, c.id DESC
```

- [ ] **Step 4: Run course tests**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_course_catalog -v
```

Expected: All course catalog tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/courses backend/app/__init__.py backend/tests/test_course_catalog.py
git commit -m "feat(courses): 增加已上架课程聚合"
```

---

### Task 8: Frontend API, Auth Store, and Role Router

**Files:**
- Modify: `frontend/package.json`
- Modify: `frontend/package-lock.json`
- Create: `frontend/vitest.config.ts`
- Modify: `frontend/src/api/types.ts`
- Modify: `frontend/src/api/client.ts`
- Modify: `frontend/src/stores/auth.ts`
- Create: `frontend/src/router/roleRoutes.ts`
- Create: `frontend/src/router/roleRoutes.test.ts`
- Create: `frontend/src/stores/auth.test.ts`
- Modify: `frontend/src/router/index.ts`

**Interfaces:**
- Consumes: backend `/api/auth/*` contracts from Tasks 2-3.
- Produces: `UserRole`; `AuthUser`; `AuthSessionResponse`; `ApiFieldErrors`; `ApiError`; `apiFetch<T>(path, options) -> Promise<T>`; `roleDefaultPath(role: UserRole) -> string`; `useAuthStore()` with `restoreSession`, `login`, `register`, `completeInterestTags`, `logout`.

- [ ] **Step 1: Add Vitest and write failing tests**

Add scripts/dependencies:

```json
{
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
```

Create `frontend/vitest.config.ts`:

```ts
import { fileURLToPath, URL } from 'node:url'
import vue from '@vitejs/plugin-vue'
import { defineConfig } from 'vitest/config'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  },
  test: {
    environment: 'jsdom'
  }
})
```

Create `frontend/src/router/roleRoutes.test.ts`:

```ts
import { describe, expect, it } from 'vitest'

import { roleDefaultPath } from './roleRoutes'

describe('roleDefaultPath', () => {
  it.each([
    ['student', '/student'],
    ['teacher', '/teacher'],
    ['enterprise', '/enterprise'],
    ['government', '/government'],
    ['super_admin', '/admin'],
    ['admin', '/admin']
  ] as const)('maps %s to %s', (role, expected) => {
    expect(roleDefaultPath(role)).toBe(expected)
  })
})
```

Create `frontend/src/stores/auth.test.ts` to mock `apiFetch` and verify restore/login/logout. Assert pending student registration does not set `user` and sets `nextStep='interest-tags'`.

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
cd frontend
npm install
npm test
```

Expected: FAIL because `roleRoutes.ts` and the new auth contract do not exist.

- [ ] **Step 3: Implement API and auth contracts**

Use these exact unions in `api/types.ts`:

```ts
export type UserRole =
  | 'student'
  | 'teacher'
  | 'enterprise'
  | 'government'
  | 'super_admin'
  | 'admin'

export interface AuthUser {
  id: number
  username: string
  name: string
  role: UserRole
}

export interface AuthSessionResponse {
  success: true
  state: 'pending' | 'active'
  user: AuthUser
  default_path: string
  next_step?: 'interest-tags'
}

export interface RegisterResponse {
  success: true
  next_step: 'interest-tags' | 'portal'
  user: AuthUser
  default_path: string
}

export type LearningDirection =
  | 'agriculture'
  | 'ecommerce'
  | 'handcraft'
  | 'comprehensive'

export interface StudentProfile {
  name: string
  contact: string
  learning_direction: LearningDirection
  tag_ids: number[]
}

export interface InterestTag {
  id: number
  group_key: 'crop' | 'skill' | 'job'
  name: string
}

export interface CourseSummary {
  id: number
  title: string
  direction: Exclude<LearningDirection, 'comprehensive'>
  summary: string
  teacher_name: string
  published_at: string
  interest_match: boolean
}
```

`ApiError` must expose `status` and `errors: Record<string, string>`. `apiFetch()` must:

- Use `credentials: 'include'`.
- Parse JSON and preserve backend `message` and `errors`.
- For authenticated 401, attach `redirect` if present.

Remove session storage from `auth.ts`; source truth is `/api/auth/session`.

`roleDefaultPath()`:

```ts
export function roleDefaultPath(role: UserRole): string {
  if (role === 'super_admin' || role === 'admin') return '/admin'
  return `/${role}`
}
```

- [ ] **Step 4: Implement the router guard tests and guard**

Route metadata:

```ts
export interface AuthRouteMeta {
  requiresAuth?: boolean
  roles?: UserRole[]
}
```

Guard behavior:

- Call `restoreSession()` once when session is unknown.
- Unauthenticated protected route → `/login?redirect=<encoded-fullPath>`.
- Authenticated user entering `/login` or `/register` → `roleDefaultPath(role)`.
- Wrong role → `roleDefaultPath(role)`.
- Pending enrollment route `/register/interest-tags` is allowed only when store has a pending registration.

- [ ] **Step 5: Run frontend tests and typecheck**

Run:

```powershell
cd frontend
npm test
npx tsc -b --noEmit
```

Expected: All tests pass and TypeScript reports no errors.

- [ ] **Step 6: Commit**

```powershell
git add frontend/package.json frontend/package-lock.json frontend/vitest.config.ts frontend/src/api frontend/src/stores/auth.ts frontend/src/stores/auth.test.ts frontend/src/router
git commit -m "feat(frontend): 接入认证会话与角色路由"
```

---

### Task 9: Login, Registration, and Interest-Tag Pages

**Files:**
- Create: `frontend/src/views/LoginView.vue`
- Create: `frontend/src/views/RegisterView.vue`
- Create: `frontend/src/views/InterestTagsView.vue`
- Create: `frontend/src/components/FormField.vue`
- Create: `frontend/src/views/RegisterView.test.ts`
- Create: `frontend/src/views/InterestTagsView.test.ts`
- Modify: `frontend/src/router/index.ts`
- Modify: `frontend/src/components/AppHeader.vue`
- Modify: `frontend/src/views/HomeView.vue`
- Delete: `frontend/src/components/AuthDialog.vue`

**Interfaces:**
- Consumes: `useAuthStore()` from Task 8; backend exact errors from Tasks 2-4.
- Produces: `/login`; `/register`; `/register/interest-tags`; field-level error/focus behavior; student saves/skips before entering `/student`.

- [ ] **Step 1: Write failing view tests**

`RegisterView.test.ts` must assert:

```ts
it('focuses the first invalid field and shows backend errors', async () => {
  const wrapper = mount(RegisterView, { global: { plugins: [pinia] } })
  await wrapper.get('#register-username').setValue('1bad')
  await wrapper.get('form').trigger('submit')
  await flushPromises()

  expect(wrapper.get('#register-username-error').text()).toContain('用户名格式不正确')
  expect(document.activeElement?.id).toBe('register-username')
})
```

`InterestTagsView.test.ts` must assert:

```ts
it('can skip without selecting any tags and routes to student portal', async () => {
  const push = vi.spyOn(router, 'push')
  const wrapper = mount(InterestTagsView, { global: { plugins: [pinia, router] } })
  await wrapper.get('[data-test="skip-tags"]').trigger('click')
  await flushPromises()
  expect(push).toHaveBeenCalledWith('/student')
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
cd frontend
npm test -- src/views/RegisterView.test.ts src/views/InterestTagsView.test.ts
```

Expected: FAIL because the views do not exist.

- [ ] **Step 3: Implement the pages**

`LoginView.vue`:

- Fields: username, password.
- Generic credential failure uses the backend message verbatim.
- Disabled account uses the backend message verbatim.
- “忘记密码” button reveals only `请联系管理员重置密码`.
- On success route to `response.default_path` or validated `redirect`.

`RegisterView.vue`:

- Role control offers exactly 学员 and 教师.
- Fields: username, password, confirm password, name.
- Keep entered values after failure.
- Map `ApiError.errors` to the matching `FormField`.
- Focus first invalid field.
- Teacher success → `/teacher`.
- Student success → `/register/interest-tags`.

`InterestTagsView.vue`:

- Load `/api/interest-tags`.
- Render three groups in fixed order: 作物类目、技能兴趣、岗位类别.
- Multi-select checkboxes.
- “保存并进入” sends selected IDs.
- “跳过” sends `[]`.
- Both actions call `auth.completeInterestTags()` then route `/student`.

Update header login/register controls to route to pages. Remove `AuthDialog` usage from `HomeView.vue`.

- [ ] **Step 4: Run view tests and build**

Run:

```powershell
cd frontend
npm test -- src/views/RegisterView.test.ts src/views/InterestTagsView.test.ts
npm run build
```

Expected: Tests PASS and production build succeeds.

- [ ] **Step 5: Commit**

```powershell
git add frontend/src/views frontend/src/components frontend/src/router/index.ts
git rm frontend/src/components/AuthDialog.vue
git commit -m "feat(frontend): 实现注册登录与兴趣标签页"
```

---

### Task 10: Role Portals and First-Use Overlay

**Files:**
- Create: `frontend/src/data/portal-guides.ts`
- Create: `frontend/src/components/OnboardingOverlay.vue`
- Create: `frontend/src/components/PortalShell.vue`
- Create: `frontend/src/views/StudentPortalView.vue`
- Create: `frontend/src/views/TeacherPortalView.vue`
- Create: `frontend/src/views/EnterprisePortalView.vue`
- Create: `frontend/src/views/GovernmentPortalView.vue`
- Create: `frontend/src/views/AdminPortalView.vue`
- Create: `frontend/src/components/OnboardingOverlay.test.ts`
- Modify: `frontend/src/router/index.ts`

**Interfaces:**
- Consumes: `/api/onboarding/<portal>`; `roleDefaultPath()` from Task 8.
- Produces: five role portal routes; `PortalShell`; overlay `complete` and `skip` events; no completion request on close/route leave.

- [ ] **Step 1: Write failing overlay test**

```ts
it('emits complete only after the final step', async () => {
  const wrapper = mount(OnboardingOverlay, {
    props: { title: '学员入口', steps: [
      { selector: '#student-courses', title: '课程', description: '浏览课程' },
      { selector: '#student-profile', title: '资料', description: '维护资料' }
    ] }
  })
  await wrapper.get('[data-test="next-step"]').trigger('click')
  expect(wrapper.emitted('complete')).toBeUndefined()
  await wrapper.get('[data-test="finish"]').trigger('click')
  expect(wrapper.emitted('complete')).toHaveLength(1)
})

it('emits skipped without emitting complete', async () => {
  const wrapper = mount(OnboardingOverlay, {
    props: {
      title: '学员入口',
      steps: [
        { selector: '#student-courses', title: '课程', description: '浏览课程' },
        { selector: '#student-profile', title: '资料', description: '维护资料' }
      ]
    }
  })
  await wrapper.get('[data-test="skip"]').trigger('click')
  expect(wrapper.emitted('skipped')).toHaveLength(1)
  expect(wrapper.emitted('complete')).toBeUndefined()
})
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
cd frontend
npm test -- src/components/OnboardingOverlay.test.ts
```

Expected: FAIL because `OnboardingOverlay.vue` does not exist.

- [ ] **Step 3: Implement portal shell and exact guide steps**

Portal mapping:

```ts
export const rolePortal: Record<UserRole, PortalId> = {
  student: 'student',
  teacher: 'teacher',
  enterprise: 'enterprise',
  government: 'government',
  super_admin: 'admin',
  admin: 'admin'
}
```

`PortalShell.vue`:

- Renders role title and portal entry links.
- On mount requests onboarding state for its portal.
- Displays `OnboardingOverlay` once when `required=true`.
- `complete` and `skipped` events POST the respective outcome, then hide overlay.
- Closing overlay, refreshing, or route leave performs no POST.

Guide entries:

- Student: 课程聚合、个人资料、学习方向入口.
- Teacher: 课程发布、教学公告、数据看板.
- Enterprise: 职位发布、申请处理、企业看板.
- Government: 政策发布、新闻发布、政务看板.
- Admin: 账号管理、内容审核、系统配置.

Create minimal but usable portal shells. Portal business actions may show non-interactive future-entry markers only where the target module is explicitly out of scope; do not implement modules 02-12.

- [ ] **Step 4: Run component tests and build**

Run:

```powershell
cd frontend
npm test -- src/components/OnboardingOverlay.test.ts
npm run build
```

Expected: Overlay tests PASS and production build succeeds.

- [ ] **Step 5: Commit**

```powershell
git add frontend/src/data/portal-guides.ts frontend/src/components/OnboardingOverlay.vue frontend/src/components/PortalShell.vue frontend/src/views/StudentPortalView.vue frontend/src/views/TeacherPortalView.vue frontend/src/views/EnterprisePortalView.vue frontend/src/views/GovernmentPortalView.vue frontend/src/views/AdminPortalView.vue frontend/src/router/index.ts frontend/src/components/OnboardingOverlay.test.ts
git commit -m "feat(frontend): 增加角色门户与新手引导"
```

---

### Task 11: Student Profile and Course Catalog UI

**Files:**
- Create: `frontend/src/stores/student.ts`
- Create: `frontend/src/views/StudentProfileView.vue`
- Create: `frontend/src/views/CourseCatalogView.vue`
- Create: `frontend/src/stores/student.test.ts`
- Create: `frontend/src/views/CourseCatalogView.test.ts`
- Modify: `frontend/src/router/index.ts`
- Modify: `frontend/src/views/StudentPortalView.vue`

**Interfaces:**
- Consumes: profile API from Task 5, course API from Task 7, auth store from Task 8, portal shell from Task 10.
- Produces: `/student/profile`; `/student/courses`; store actions `loadProfile`, `saveProfile`, `loadCourses`.

- [ ] **Step 1: Write failing store/view tests**

```ts
it('updates profile then refreshes recommendations from new preference values', async () => {
  mockedApiFetch
    .mockResolvedValueOnce({ success: true, profile: profileFixture })
    .mockResolvedValueOnce({ success: true, profile: { ...profileFixture, learning_direction: 'ecommerce' } })
    .mockResolvedValueOnce({ success: true, courses: recommendedAfterUpdate })

  const store = useStudentStore()
  await store.loadProfile()
  await store.saveProfile({ ...profileFixture, learning_direction: 'ecommerce' })
  await store.loadCourses('ecommerce')

  expect(store.courses[0].interest_match).toBe(true)
})

it('renders the exact empty state', async () => {
  const wrapper = mount(CourseCatalogView, { global: { plugins: [pinia] } })
  await wrapper.get('[data-test="direction-handcraft"]').trigger('click')
  await flushPromises()
  expect(wrapper.text()).toContain('暂无课程')
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
cd frontend
npm test -- src/stores/student.test.ts src/views/CourseCatalogView.test.ts
```

Expected: FAIL because the store/views do not exist.

- [ ] **Step 3: Implement profile and course views**

`StudentProfileView.vue`:

- Name, contact, direction segment, three tag groups.
- Direction options and labels are exact.
- Save calls `PUT /api/student/profile`.
- Successful save updates the auth user name and student store.
- Show backend field errors inline.

`CourseCatalogView.vue`:

- Three tabs: 农业、电商、手工.
- Default tab is agriculture.
- Loading, error, list and exact empty state.
- Course item shows title, teacher, summary, published time and a visible “推荐” marker when `interest_match=true`.
- Switching tab re-fetches from `/api/student/courses`.

- [ ] **Step 4: Run tests and build**

Run:

```powershell
cd frontend
npm test -- src/stores/student.test.ts src/views/CourseCatalogView.test.ts
npm run build
```

Expected: Tests PASS and production build succeeds.

- [ ] **Step 5: Commit**

```powershell
git add frontend/src/stores/student.ts frontend/src/stores/student.test.ts frontend/src/views/StudentProfileView.vue frontend/src/views/CourseCatalogView.test.ts frontend/src/views/CourseCatalogView.vue frontend/src/views/StudentPortalView.vue frontend/src/router/index.ts
git commit -m "feat(frontend): 实现学员资料与课程聚合"
```

---

### Task 12: Local Seed, Cross-Service Integration, and Browser Acceptance

**Files:**
- Create: `backend/app/seed_dev.py`
- Create: `backend/tests/test_integration_user_portal.py`
- Create: `backend/.env.example`
- Modify: `backend/app/seed.py`
- Modify: `README.md`

**Interfaces:**
- Consumes: all interfaces from Tasks 1-11.
- Produces: repeatable local six-role seed; end-to-end API acceptance; desktop and mobile browser evidence.

- [ ] **Step 1: Write failing cross-service acceptance test**

`backend/tests/test_integration_user_portal.py` must execute this sequence against one app:

```python
def test_complete_user_portal_journey(self):
    # student registration -> interest page -> skip -> profile
    register = self.client.post("/api/auth/register", json=student_payload)
    self.assertEqual(register.get_json()["next_step"], "interest-tags")
    self.client.post("/api/auth/interest-tags", json={"tag_ids": []})
    self.assertEqual(self.client.get("/api/auth/session").status_code, 200)

    # profile update -> tagged course ranks first
    tag_id = self.create_tag("crop", "荔枝")
    self.seed_course(
        title="荔枝保果",
        direction="agriculture",
        status="published",
        published_at="2026-09-01T00:00:00+00:00",
        tag_ids=[tag_id],
    )
    self.client.put("/api/student/profile", json=profile_payload)
    courses = self.client.get(
        "/api/student/courses?direction=agriculture"
    ).get_json()["courses"]
    self.assertTrue(courses[0]["interest_match"])

    # all six roles route to expected portals
    for role, expected in ROLE_PATHS.items():
        # create through test helper, not registration
        self.login_role(role)
        self.assertEqual(self.client.get("/api/auth/session").get_json()["default_path"], expected)
```

- [ ] **Step 2: Run integrated backend suite to verify targeted failure**

Run:

```powershell
uv run --directory backend python -m unittest tests.test_integration_user_portal -v
```

Expected: FAIL on the first missing cross-task behavior.

- [ ] **Step 3: Add safe local seed and close integration gaps**

`backend/app/seed_dev.py` must:

- Require `DEV_SEED_PASSWORD` and `SECRET_KEY` from environment.
- Refuse to run when `FLASK_ENV=production`.
- Upsert one enabled account per role using `DEV_SEED_<ROLE>_USERNAME`.
- Seed the default tag catalog and a few published courses.
- Never print passwords.
- Run with `uv run --directory backend python -m app.seed_dev`.

`backend/.env.example` contains variable names and empty values only.

Define the integration helper with concrete SQL in the same test file:

```python
def seed_course(
    self,
    title: str,
    direction: str,
    status: str,
    published_at: str,
    tag_ids: list[int],
) -> int:
    with self.app.app_context():
        db = get_db()
        cursor = db.execute(
            """
            INSERT INTO courses (
                title, direction, status, published_at, summary,
                teacher_name, created_at, updated_at
            ) VALUES (?, ?, ?, ?, '', '测试教师', ?, ?)
            """,
            (
                title,
                direction,
                status,
                published_at,
                "2026-09-01T00:00:00+00:00",
                "2026-09-01T00:00:00+00:00",
            ),
        )
        course_id = cursor.lastrowid
        db.executemany(
            "INSERT INTO course_interest_tags (course_id, tag_id) VALUES (?, ?)",
            [(course_id, tag_id) for tag_id in tag_ids],
        )
        db.commit()
        return course_id
```

- [ ] **Step 4: Run all automated gates**

Run:

```powershell
uv run --directory backend python -m unittest discover -s tests -v
cd frontend
npm test
npx tsc -b --noEmit
npm run build
```

Expected: All backend and frontend tests pass; typecheck and build pass.

- [ ] **Step 5: Start both services and run browser acceptance**

Run backend and frontend in separate hidden background sessions:

```powershell
uv run --directory backend python run.py
cd frontend
npm run dev
```

Use `agent-browser` to verify:

1. Student registration validation and duplicate username.
2. Student registration → save/skip tags → student portal.
3. Teacher registration → teacher portal.
4. Invalid credentials, disabled account, forgot-password message.
5. Expired session redirect and post-login target restoration.
6. Onboarding complete, skip, and interrupted-close states.
7. Profile direction/tag update → course recommendation reorders.
8. Agriculture/e-commerce/handcraft tabs, published-only filtering, recommendation marker, and “暂无课程”.
9. Desktop and 375px mobile screenshots for login, registration, interest tags, student portal, guide, profile, and courses.

Expected: All flows pass with no console errors, no overlapping content, and no horizontal overflow.

- [ ] **Step 6: Commit**

```powershell
git add backend/app/seed.py backend/app/seed_dev.py backend/tests/test_integration_user_portal.py backend/.env.example README.md
git commit -m "test(portal): 完成账户门户集成验收"
```

---

## Plan Self-Review

### Spec Coverage

| Spec area | Implementation tasks |
| --- | --- |
| FR-001..FR-014 student/teacher registration | Tasks 1-4, 8-9 |
| FR-015..FR-024 login, session, routing, login errors | Tasks 1-3, 8-10 |
| FR-025..FR-029 first-use guidance | Tasks 6, 10 |
| FR-030..FR-035 student profile and preference propagation | Tasks 4-5, 8, 11 |
| FR-036..FR-041 course aggregation | Tasks 1, 5, 7-8, 11 |
| SC-001..SC-009 measurable acceptance | Tasks 2-7 automated tests, Task 12 integration/browser |
| Constitution security, TDD, review, naming, rewrites | Global Constraints and Execution Contract |

### Interface Consistency

- `create_app()` is defined in Task 1 and used only as the app factory in every later backend task.
- Session cookie name is always `yx_session`; session states are always `pending` and `active`.
- Exact login messages and exact UI empty/error strings are copied from the spec.
- Learning direction union is identical in SQLite, backend payloads and TypeScript.
- `get_profile_preferences()` is the only cross-module profile contract used by course ranking.
- Course ordering is always `interest_match DESC`, `published_at DESC`, `id DESC`.
- Frontend API DTO field names remain snake_case to match backend JSON.

### Incomplete-Work Scan

- No prohibited incomplete-work or vague-implementation markers remain.
- Every implementation task has concrete files, interfaces, tests, commands, expected results, and a commit boundary.

### Approval Gate

This plan is ready for user review. Implementation must not begin until the user explicitly approves it.
